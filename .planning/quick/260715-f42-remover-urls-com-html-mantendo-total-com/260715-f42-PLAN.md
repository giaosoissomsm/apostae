---
phase: quick-260715-f42
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
  - public/index.html
  - public/admin.html
  - public/profile.html
  - public/login.html
  - public/password-expires.html
  - public/js/api.js
  - public/js/session-timeout.js
  - public/js/password-expires.js
  - src/middleware/auth.js
autonomous: true
requirements:
  - F42-CLEAN-URLS

must_haves:
  truths:
    - "GET /admin.html returns HTTP 301 with Location /admin (query string preserved verbatim)"
    - "GET /admin serves admin.html (the real admin panel), correcting today's bug where /admin serves the dashboard shell"
    - "GET / serves index.html, /login serves login.html, /cadastro serves login.html, /profile serves profile.html, /password-expires serves password-expires.html — all HTTP 200"
    - "Static assets (/css/style.css, /js/*.js) and API routes (/api/*, /health) respond exactly as before"
    - "An unmapped .html path (e.g. /foo.html) returns a clean JSON 404, not a loop or crash"
    - "A 401 on any page redirects the browser to /login exactly once with no redirect loop"
  artifacts:
    - "server.js containing a /\\.html$/i 301-redirect route before express.static, express.static with { index: false }, and a CLEAN_TO_FILE explicit route map"
    - "public/*.html and public/js/*.js with all navigation targets pointing at extensionless clean URLs"
    - "<link rel=\"canonical\"> tag present on all five page HTML files"
  key_links:
    - "The /\\.html$/i redirect route MUST be registered ABOVE express.static, or static serves the .html with a 200 and the redirect never fires"
    - "api.js and session-timeout.js login-page guards MUST compare against the new extensionless paths (/login, /password-expires) — a stale .html comparison makes the guard return false and causes a /login -> /login redirect loop (highest-risk regression)"
    - "No catch-all page route is introduced — the explicit CLEAN_TO_FILE whitelist must not shadow /css/*, /js/*, /api/*, or /health"
---

<objective>
Remove `.html` extensions from all public URLs in ApostaE while keeping full backward
compatibility: legacy `*.html` URLs 301-redirect to their clean equivalents, and every
internal navigation reference (hrefs, inline scripts, page-detection guards) targets the new
extensionless paths. Also correct the pre-existing bug where `/admin` serves the dashboard
shell instead of the admin panel.

Purpose: Cleaner, canonical URLs with permanent redirects preserving existing bookmarks and
query strings; no new dependency.
Output: Updated server-side routing in server.js and all 18 internal reference updates across
6 files, plus canonical link tags on 5 pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260715-f42-remover-urls-com-html-mantendo-total-com/260715-f42-RESEARCH.md
@server.js
@public/js/api.js
@public/js/session-timeout.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side clean-URL routing in server.js</name>
  <files>server.js</files>
  <action>
Restructure the FRONTEND ESTÁTICO block (currently server.js lines 93-102) to introduce clean
URLs. Follow the research's recommended implementation exactly and preserve route ordering:

1. BEFORE the express.static call, register a single legacy-to-clean redirect route matched by
   the regex `/\.html$/i`. Inside it, look up the request path in a map of legacy page paths to
   clean targets: index page -> `/`, login page -> `/login`, admin page -> `/admin`, profile
   page -> `/profile`, password-expires page -> `/password-expires`. If the path is not in the
   map, call next() so it falls through to the existing JSON 404 handler (never a loop, never a
   crash). If it is in the map, preserve the query string by appending
   `req.originalUrl.slice(req.path.length)` (yields the raw `?a=b` or an empty string) and call
   `res.redirect(301, target + qs)`.

2. Change the express.static call to pass the option object making it not auto-serve the index
   file (`{ index: false }`), so express.static never emits the index page at `/`; the explicit
   route below owns `/` as the single source of truth.

3. REPLACE the vestigial SPA route at line 100 (the `app.get(['/', '/dashboard', '/admin',
   '/profile'], ...)` that wrongly sends the index shell for `/admin`) with an explicit
   clean-URL to file map and a loop registering one `app.get(route, ...)` per entry that
   `res.sendFile`s the corresponding file from public/: `/` -> index page, `/login` -> login
   page, `/cadastro` -> login page (registration is an in-page toggle, no separate file),
   `/admin` -> admin page, `/profile` -> profile page, `/password-expires` -> password-expires
   page. Drop `/dashboard` (not a real page today). Never interpolate req.path into the filename
   — use the fixed whitelist values only.

Do NOT add a catch-all `app.get('*')` page route (it would shadow /css, /js, /api, /health).
Do NOT add any npm dependency. Note in a brief inline comment that URL hash fragments are
browser-only and never reach the server, so `#...` preservation is automatic and needs no code.
  </action>
  <verify>
    <automated>node -e "require('./server.js')" 2>/dev/null; node --check server.js &amp;&amp; grep -q "index: false" server.js &amp;&amp; grep -Eq "redirect\(301" server.js &amp;&amp; echo OK</automated>
  </verify>
  <done>server.js parses (node --check passes); the regex redirect route appears above express.static; express.static uses { index: false }; the old SPA app.get on line ~100 is replaced by the CLEAN_TO_FILE per-route loop including /cadastro -> login page and /admin -> admin page.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint HTML hrefs to clean URLs and add canonical tags</name>
  <files>public/index.html, public/admin.html, public/profile.html, public/login.html, public/password-expires.html</files>
  <action>
Apply exactly these href swaps (each is a nav link to another page):
- index.html:18 admin-page href -> `/admin`
- index.html:19 profile-page href -> `/profile`
- admin.html:15 index-page href -> `/`
- admin.html:16 profile-page href -> `/profile`
- profile.html:15 index-page href -> `/`
- profile.html:16 admin-page href -> `/admin`
- profile.html:17 profile-page href -> `/profile`

Also update these two inline-script navigation targets in login.html (both are post-auth
redirects sending the user to the dashboard):
- login.html:49 `location.href = '/index.html'` -> `/`
- login.html:80 `location.href = '/index.html'` -> `/`

Then add a canonical link tag inside the `<head>` of each of the five pages (place it near the
existing stylesheet link on line 9), pointing each page at its OWN clean URL:
- index.html -> canonical `/`
- admin.html -> canonical `/admin`
- profile.html -> canonical `/profile`
- password-expires.html -> canonical `/password-expires`
- login.html -> canonical `/login` (deliberately `/login`, NOT `/cadastro`: /login and
  /cadastro serve identical markup, so login is the single canonical target to avoid
  duplicate-content flagging)

Change only the href attributes listed above, the two enumerated login.html inline-script
`location.href` navigation targets (lines 49 and 80), and the canonical tags — do not touch the
CSS/font links, titles, or page body markup. The "do not touch scripts" restriction means: do
not alter any OTHER script content or logic (event handlers, form submission, register-toggle
behavior, etc.); the two enumerated `'/index.html'` navigation-target literals ARE explicitly in
scope and must be swapped to `/`.
  </action>
  <verify>
    <automated>grep -c 'rel="canonical"' public/index.html public/admin.html public/profile.html public/login.html public/password-expires.html | grep -vq ':0' &amp;&amp; ! grep -nE 'href="/[a-z-]+\.html"' public/index.html public/admin.html public/profile.html &amp;&amp; ! grep -nF "location.href = '/index.html'" public/login.html &amp;&amp; echo OK</automated>
  </verify>
  <done>All seven nav hrefs use extensionless paths; both login.html inline-script redirects (lines 49, 80) now target `/` with no remaining `location.href = '/index.html'` in login.html; no page-nav href in index/admin/profile still ends in the old extension; each of the five pages has exactly one canonical tag pointing at its own clean URL (login canonical is /login).</done>
</task>

<task type="auto">
  <name>Task 3: Update JS navigation and page-detection guards (loop-prevention)</name>
  <files>public/js/api.js, public/js/session-timeout.js, public/js/password-expires.js, src/middleware/auth.js</files>
  <action>
Update every client-side navigation target and — critically — the two page-detection guards
(Pitfall 2, the highest-risk change). Get the guard comparison logic exactly right, since a
wrong guard causes a /login -> /login redirect loop.

public/js/api.js:
- Line 34 guard: replace the two `location.pathname.endsWith(...)` legacy-page checks so they
  test the new extensionless paths. Use `location.pathname === '/login'` for the login check and
  `location.pathname === '/password-expires'` for the password-expires check (exact match, not
  endsWith — the clean paths have no extension to anchor on). The guard's meaning is unchanged:
  "don't redirect if already on the login or password-expires page."
- Line 35 login redirect target -> `/login`
- Line 42 password-expires redirect target -> `/password-expires`
- Line 65 (requireLogin) login redirect target -> `/login`
- Line 81 (requireAdminPage non-admin bounce) index redirect target -> `/`
- Line 108 (logout) login redirect target -> `/login`

public/js/session-timeout.js:
- Line 12 guard: replace the `location.pathname.includes('login...')` /
  `.includes('password-expires...')` early-return so it tests the clean paths, e.g.
  `location.pathname === '/login' || location.pathname === '/password-expires'`. Same intent:
  the inactivity timer must not run on the login or password-expires page.

public/js/password-expires.js:
- Line 6 index redirect target -> `/`
- Line 37 login redirect target -> `/login`

src/middleware/auth.js:
- Line 65 isPasswordChangeRoute literal `/password-expires.html` -> `/password-expires` (this
  branch is effectively dead because requireAuth is only mounted on /api/* routers, so this is a
  no-op consistency fix — update it anyway so no stale extension path names remain in the tree).

Do not change any other logic in these files.
  </action>
  <verify>
    <automated>node --check public/js/api.js &amp;&amp; node --check public/js/session-timeout.js &amp;&amp; node --check public/js/password-expires.js &amp;&amp; node --check src/middleware/auth.js &amp;&amp; ! grep -rnE "location\.(href|pathname)[^)]*[a-z-]+\.html" public/js/ &amp;&amp; echo OK</automated>
  </verify>
  <done>All four files parse; both page-detection guards (api.js line 34, session-timeout.js line 12) compare against the extensionless paths so no redirect loop can occur; every JS navigation target uses a clean URL; auth.js line 65 literal updated. No `location.href`/`location.pathname` reference in public/js still names a legacy extension page.</done>
</task>

</tasks>

<verification>
Run with the server started on the configured PORT (env `PORT`, default 3000 per .env). Substitute the actual port if different.

Redirect + query preservation (301, permanent):
- `curl -sI http://localhost:3000/admin.html` → `301` + `Location: /admin`
- `curl -sI 'http://localhost:3000/login.html?next=x'` → `Location: /login?next=x` (query kept verbatim)
- `curl -sI http://localhost:3000/index.html` → `301` + `Location: /`
- `curl -sI http://localhost:3000/profile.html` → `Location: /profile`
- `curl -sI http://localhost:3000/password-expires.html` → `Location: /password-expires`

Clean URLs serve the right page (the /admin fix is the key correction):
- `curl -s http://localhost:3000/admin` → serves admin panel markup (admin.html), NOT the dashboard as it did before this task
- `curl -s http://localhost:3000/` → serves index.html
- `curl -s http://localhost:3000/login` and `curl -s http://localhost:3000/cadastro` → both serve login.html
- `curl -s http://localhost:3000/profile` → serves profile.html
- `curl -s http://localhost:3000/password-expires` → serves password-expires.html

Assets / API unaffected, and unmapped .html degrades cleanly (no loop, no crash):
- `curl -sI http://localhost:3000/css/style.css` → `200`
- `curl -sI http://localhost:3000/js/api.js` → `200`
- `curl -sI http://localhost:3000/api/health` (or `/health`) → unchanged from before
- `curl -si http://localhost:3000/foo.html` → JSON `404` (`{ "error": "Rota não encontrada" }`), single response, no redirect chain

Static structure gates:
- `grep -n "index: false" server.js` (express.static must not auto-emit the index)
- `! grep -q "app.get(\['/', '/dashboard'" server.js` (old SPA route removed)
- `grep -c 'rel="canonical"' public/*.html` (canonical tag on all five pages)

Manual (browser):
- Login flow, then a forced 401 → browser redirects to `/login` exactly once (NO `/login` → `/login` loop). This is the highest-risk regression; confirm explicitly.
- Non-admin loads `/admin` → client-side check bounces to `/` (not `/index.html`).
- Password-expires forced-change flow reaches `/password-expires` and, on success, `/login`.
- F5 (hard refresh) on each clean URL still serves the page; deep-link directly to `/profile` works.
- A bookmarked URL with a `#hash` (e.g. `/profile#settings`) keeps its hash after the 301 (browser re-appends automatically).
</verification>

<success_criteria>
- Every legacy `*.html` public page URL 301-redirects to its clean equivalent with query string preserved.
- `/admin` serves the admin panel (bug corrected); `/`, `/login`, `/cadastro`, `/profile`, `/password-expires` serve their pages.
- All 18 enumerated internal references updated; both login-page guards match the new paths (no redirect loop).
- Static assets, `/api/*`, `/health` unchanged; unmapped `.html` returns a clean JSON 404.
- Canonical link tag on all five pages (login → `/login`).
- No new npm dependency added.
</success_criteria>

<output>
Create `.planning/quick/260715-f42-remover-urls-com-html-mantendo-total-com/260715-f42-SUMMARY.md` when done
</output>