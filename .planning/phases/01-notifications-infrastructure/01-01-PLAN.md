---
phase: 01-notifications-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - jest.config.js
  - tests/helpers/testDb.js
  - tests/notifications.ownership.test.js
  - tests/notifications.idempotency.test.js
  - tests/notifications.pagination.test.js
  - tests/notifications.read-state.test.js
  - tests/notifications.events.test.js
  - src/migrations/003_notifications.js
  - src/events/domainEvents.js
autonomous: false
requirements: [NOTIF-09, NOTIF-10]
must_haves:
  truths:
    - "Running `npm test` executes a real Jest runner (not the echo stub) and exits 0 with no test files failing to load."
    - "A `notifications` table exists after migration 003 runs, with a UNIQUE(user_id, type, related_entity, related_id) constraint and the two required indexes."
    - "`require('../events/domainEvents')` returns the same EventEmitter singleton to every requirer, and a listener throwing never crashes the process."
  artifacts:
    - jest.config.js
    - tests/helpers/testDb.js
    - src/migrations/003_notifications.js
    - src/events/domainEvents.js
    - tests/notifications.ownership.test.js
    - tests/notifications.idempotency.test.js
    - tests/notifications.pagination.test.js
    - tests/notifications.read-state.test.js
    - tests/notifications.events.test.js
  key_links:
    - "domainEvents singleton is the future realtime-delivery swap point (NOTIF-10) — every emitter/subscriber shares one require-cached instance."
    - "testDb helper applies migration 003 and truncates notifications between tests, unblocking every integration test in later plans."
---

## Phase Goal

**As an** ApostaE bettor, **I want to** be notified of my wager and market status changes and read/manage those notifications through an ownership-scoped API, **so that** I stay informed about my bets without polling and can never see another user's notifications.

> Derived faithfully from the ROADMAP.md Phase 1 `**Goal:**` line (outcome-shaped prose); no scope invented. This is Plan 01 of the phase's vertical slices — it establishes the test harness, durable schema, and event-bus seam every later slice cuts through.

<objective>
Establish the phase's foundation: a working Jest test harness with a reusable Postgres test-DB fixture, the `notifications` table migration (idempotency constraint + keyset-ready indexes), and the shared `domainEvents` EventEmitter singleton that is the single chokepoint (NOTIF-09) and the future realtime-delivery swap point (NOTIF-10) per D-02/D-03/D-04.

Purpose: Nothing else in the phase can be built or verified without a runnable test framework (a milestone-wide gap per STATE.md), the durable table, and the event bus. This plan closes the Wave 0 validation gap from 01-VALIDATION.md.
Output: `npm test` runs Jest; `tests/helpers/testDb.js`; migration `003_notifications.js`; `src/events/domainEvents.js`; five failing/pending test stubs mapped to NOTIF requirements.
Backend only (D-07) — no `public/` changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-notifications-infrastructure/01-CONTEXT.md
@.planning/phases/01-notifications-infrastructure/01-RESEARCH.md
@.planning/phases/01-notifications-infrastructure/01-PATTERNS.md
@.planning/phases/01-notifications-infrastructure/01-VALIDATION.md
@src/migrations/002_wallet.js
@scripts/migrate.js
@src/config/database.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve test-framework dependency install (supply-chain gate)</name>
  <files>package.json, package-lock.json</files>
  <action>Blocking human-verify gate: pause before Task 2 installs the Jest devDependency. Do not run any `npm install` until the human confirms jest's legitimacy per the steps below (threat T-01-SC).</action>
  <what-built>This plan installs Jest as a devDependency. RESEARCH.md's Package Legitimacy Audit stated "no external packages this phase" but overlooked the mandated test-framework install (01-VALIDATION.md Wave 0), so this install has no pre-cleared audit row and requires explicit human approval before running (threat T-01-SC).</what-built>
  <how-to-verify>
    1. Confirm `jest` is the canonical JS test runner: open https://www.npmjs.com/package/jest — verify it is published by the OpenJS Foundation / Meta, has tens of millions of weekly downloads, and a recent release.
    2. Confirm the intended install is a devDependency only: `npm install --save-dev jest` (no runtime dependency added; no change to the production dependency list in package.json).
    3. Confirm no other new package is introduced by this phase (RESEARCH.md: all runtime capability uses Node builtins + existing deps).
  </how-to-verify>
  <resume-signal>Type "approved" to allow the Jest devDependency install, or name an alternative (e.g. Vitest) to use instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Jest, wire npm test, and build the Postgres test-DB fixture</name>
  <files>package.json, jest.config.js, tests/helpers/testDb.js</files>
  <read_first>
    - package.json (current `"test": "echo \"Add tests later\""` stub; `"type": "commonjs"`)
    - scripts/migrate.js (how migrations load: `loadMigrations()` reads `src/migrations/*.js`, each exports `{ id, up, down }`; `executeMigration` runs each `up` SQL string)
    - src/config/database.js (`pool`, `query`, `transaction` exports; NUMERIC parsed to float)
    - src/config/env.js (how DB connection env vars are read)
  </read_first>
  <action>
    Install Jest as a devDependency (per Task 1 approval) and set package.json `scripts.test` to `jest --runInBand` (serial, because integration tests share one Postgres DB). Add `scripts.test:watch` is NOT permitted — no watch mode.
    Create `jest.config.js` (CommonJS `module.exports`) with `testEnvironment: 'node'`, `testMatch: ['**/tests/**/*.test.js']`, `setupFilesAfterEnv: ['<rootDir>/tests/helpers/testDb.js']` only if the helper registers global hooks — otherwise leave setup to per-file imports.
    Create `tests/helpers/testDb.js` exporting: `applyNotificationsMigration()` (loads `src/migrations/003_notifications.js` and runs each string in its `up` array via the `query` helper from `src/config/database`), `truncateNotifications()` (`TRUNCATE notifications RESTART IDENTITY CASCADE`), and `closePool()` (`pool.end()`). CRITICAL SAFETY GUARD: the helper MUST throw and abort if `env.DB_NAME` does not contain the substring `test` — this prevents truncating the dev/prod database. Document in a header comment (Portuguese, matching codebase tone) that integration tests require a dedicated database whose name contains `test` (e.g. `apostae_test`), created/pointed-to via the standard DB_* env vars with `NODE_ENV=test`.
    Do NOT hardcode credentials; reuse `src/config/env.js` values.
  </action>
  <behavior>
    - `npm test` invokes Jest (not echo) and exits 0 when only pending/stub tests exist.
    - `testDb.applyNotificationsMigration()` creates the `notifications` table when pointed at a `*test*` database.
    - `testDb.truncateNotifications()` throws if `DB_NAME` lacks `test`.
  </behavior>
  <verify>
    <automated>npx jest --listTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"test"' package.json` shows a `jest` invocation, not `echo`.
    - `node -e "require('./tests/helpers/testDb.js')"` loads without throwing (module parses).
    - Source assertion: `tests/helpers/testDb.js` contains a guard rejecting DB names without `test`.
    - `npx jest --listTests` prints the five stub test paths and exits 0.
  </acceptance_criteria>
  <done>Jest runs via `npm test`; `tests/helpers/testDb.js` exports the migration/truncate/close helpers with the `*test*`-DB safety guard.</done>
</task>

<task type="auto">
  <name>Task 3: Create migration 003_notifications.js and the five failing test stubs</name>
  <files>src/migrations/003_notifications.js, tests/notifications.ownership.test.js, tests/notifications.idempotency.test.js, tests/notifications.pagination.test.js, tests/notifications.read-state.test.js, tests/notifications.events.test.js</files>
  <read_first>
    - src/migrations/002_wallet.js (exact export shape `{ id, up: [sqlString...], down: [...] }`; `related_entity VARCHAR(50)` / `related_id INTEGER` idiom; `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP`; `ON DELETE CASCADE` FK to users)
    - .planning/phases/01-notifications-infrastructure/01-PATTERNS.md (migration section: exact notifications DDL)
    - .planning/phases/01-notifications-infrastructure/01-VALIDATION.md (Per-Task Verification Map: which req each of the five test files covers)
  </read_first>
  <action>
    Create `src/migrations/003_notifications.js` exporting `{ id: '003_notifications', up: [...], down: ['DROP TABLE IF EXISTS notifications;'] }` with a Portuguese header comment matching 002's style ("Migration 003: Sistema de Notificações"). The `up` array holds one SQL string that creates the `notifications` table with columns: `id SERIAL PRIMARY KEY`, `user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE`, `type VARCHAR(50) NOT NULL`, `title VARCHAR(200) NOT NULL`, `body TEXT`, `related_entity VARCHAR(50)`, `related_id INTEGER`, `read_at TIMESTAMP`, `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP`, and a table-level `UNIQUE (user_id, type, related_entity, related_id)` constraint (D-03 idempotency key). Append `CREATE INDEX IF NOT EXISTS idx_notifications_user_created ON notifications(user_id, created_at DESC, id DESC);` (keyset-ready ordering, NOTIF-10) and `CREATE INDEX IF NOT EXISTS idx_notifications_user_unread ON notifications(user_id, created_at DESC) WHERE read_at IS NULL;` (fast unread-count). Use `CREATE TABLE IF NOT EXISTS`. Confirm the migration runner auto-discovers it by filename sort order (next sequential after 001/002).
    Create the five test stub files, each importing `tests/helpers/testDb`, each with a top-of-file comment naming the requirement it covers, and each containing exactly one pending test using Jest's `test.todo(...)` (or a `test(..., () => {})` marked `.skip`) so the suite loads and passes without asserting yet. Map: `tests/notifications.ownership.test.js` → NOTIF-08; `tests/notifications.idempotency.test.js` → NOTIF-09; `tests/notifications.pagination.test.js` → NOTIF-06; `tests/notifications.read-state.test.js` → NOTIF-07; `tests/notifications.events.test.js` → NOTIF-01..05. Later plans replace the todos with real assertions.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const m=require('./src/migrations/003_notifications.js'); if(m.id!=='003_notifications'||!Array.isArray(m.up)) process.exit(1)"` exits 0.
    - Source assertion: `grep -c 'UNIQUE (user_id, type, related_entity, related_id)' src/migrations/003_notifications.js` returns 1.
    - Source assertion: `grep -c 'idx_notifications_user_unread' src/migrations/003_notifications.js` returns 1.
    - `npx jest` exits 0 with 5 test files loaded (todos pending, no failures).
  </acceptance_criteria>
  <done>Migration 003 defines the notifications table with the idempotency UNIQUE constraint and both indexes; all five requirement-mapped test stubs load and the suite is green.</done>
</task>

<task type="auto">
  <name>Task 4: Create the domainEvents EventEmitter singleton with crash-safety handler</name>
  <files>src/events/domainEvents.js</files>
  <read_first>
    - .planning/phases/01-notifications-infrastructure/01-PATTERNS.md (domainEvents.js required shape; logger call signature `logger.error(msg, meta)`)
    - src/utils/logger.js (confirm `error`/`warn`/`info`/`debug` signatures)
    - .planning/phases/01-notifications-infrastructure/01-RESEARCH.md (Pitfall 1: unhandled `'error'` event crashes the process)
  </read_first>
  <action>
    Create `src/events/domainEvents.js`: require Node's builtin `events` module, construct `const domainEvents = new EventEmitter()`, register a top-level `domainEvents.on('error', (err) => require('../utils/logger').error('domainEvents listener error', err))` so an `'error'` emission can never terminate the process (threat T-01-03, Pitfall 1), and `module.exports = domainEvents`. This is a generic bus per D-02/D-06 — it defines NO fixed event-name switch; any string event can be emitted/subscribed. Do NOT add any notification-specific logic here; it is shared infrastructure future subscribers (email/push/WhatsApp) will also use (D-02).
  </action>
  <behavior>
    - Two separate `require('./src/events/domainEvents')` calls return the identical instance (Node module cache).
    - Emitting `'error'` with no domain listener does not throw/crash because the top-level handler is registered.
  </behavior>
  <verify>
    <automated>node -e "const a=require('./src/events/domainEvents');const b=require('./src/events/domainEvents');if(a!==b)process.exit(1);a.emit('error',new Error('x'));a.emit('probe',{ok:true});console.log('singleton+error-safe OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the verify command prints `singleton+error-safe OK` and exits 0 (same instance; `emit('error')` does not crash).
    - Source assertion: `grep -c "on('error'" src/events/domainEvents.js` returns 1.
    - Source assertion: `src/events/domainEvents.js` contains no notification-domain identifiers (no `notification`, no event-name switch) — it is generic.
  </acceptance_criteria>
  <done>`domainEvents` is a shared singleton EventEmitter with a process-safe top-level `'error'` handler and no domain coupling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Test-framework devDependency install crosses an untrusted supply-chain boundary |
| any emitter → domainEvents bus | A listener bug/rejection must not cross back and crash unrelated financial code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm install (jest devDependency) | high | mitigate | Blocking human checkpoint (Task 1) verifying jest legitimacy on npmjs.com before install; devDependency only; no runtime deps added |
| T-01-03 | Denial of Service | src/events/domainEvents.js | high | mitigate | Top-level `.on('error', ...)` handler registered at module load (Task 4) so an `'error'` emission cannot terminate the process (Pitfall 1) |
| T-01-02 | Denial of Service (inbox spam) | notifications table | medium | mitigate | Schema-level `UNIQUE(user_id, type, related_entity, related_id)` constraint (Task 3) is the durable, race-free dedup basis consumed by Plan 02 |
</threat_model>

<verification>
- `npm test` runs Jest and the five stub files load green.
- Migration 003 defines the notifications table, idempotency UNIQUE constraint, and both indexes.
- `domainEvents` is a crash-safe shared singleton.
- No `public/` files changed (D-07).
</verification>

<success_criteria>
- Jest installed (devDependency, human-approved), `npm test` green with 5 loaded stubs.
- `tests/helpers/testDb.js` applies migration 003 and truncates safely (guarded to `*test*` DBs).
- `src/migrations/003_notifications.js` and `src/events/domainEvents.js` exist and satisfy the source assertions above.
</success_criteria>

## Artifacts this phase produces (Plan 01)

New files: `jest.config.js`, `tests/helpers/testDb.js`, `src/migrations/003_notifications.js`, `src/events/domainEvents.js`, and five test stubs (`tests/notifications.{ownership,idempotency,pagination,read-state,events}.test.js`).
New symbols: `domainEvents` (EventEmitter singleton, default export of `src/events/domainEvents.js`); migration object `{ id: '003_notifications', up, down }`; DB table `notifications` with columns `id, user_id, type, title, body, related_entity, related_id, read_at, created_at`, constraint `notifications_user_id_type_related_entity_related_id_key` (UNIQUE), indexes `idx_notifications_user_created`, `idx_notifications_user_unread`; test helper exports `applyNotificationsMigration`, `truncateNotifications`, `closePool`; package.json `scripts.test` = jest.

<output>
Create `.planning/phases/01-notifications-infrastructure/01-01-SUMMARY.md` when done.
</output>
