---
phase: 04-bet-cancellation-v2
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/cancel.happy-path.test.js
  - tests/cancel.fee-computation.test.js
  - tests/cancel.audit.test.js
autonomous: true
requirements: [CANCEL-01, CANCEL-02, CANCEL-03, CANCEL-04, CANCEL-05]
user_setup: []

must_haves:
  truths:
    - "An integration test proves a clean pending-wager cancellation refunds 95% net, charges 5% fee, and sets status 'refunded' (CANCEL-01, CANCEL-02, CANCEL-05)"
    - "A test proves the fee is computed off remaining stake and matches money.applyFeePercent at CANCEL_FEE_PERCENT (CANCEL-02, CANCEL-03)"
    - "A test proves exactly one wallet_transactions 'refund' row is written and reconciles balance_before/balance_after with the real wallet balance (CANCEL-04)"
  artifacts:
    - tests/cancel.happy-path.test.js
    - tests/cancel.fee-computation.test.js
    - tests/cancel.audit.test.js
  key_links:
    - "tests drive the real, unmodified wagerService.cancelWager against a seeded pending wager"
    - "tests reuse tests/helpers/testDb.js seed helpers (seedTestUser/seedWallet/seedOpenMarket/seedWager)"
---

<objective>
Prove the positive path of the rewritten `cancelWager`: a user can cancel a clean pending wager, is charged a 5% fee and refunded 95% of the remaining stake, the status becomes 'refunded', and exactly one reconciling wallet_transactions audit row is produced.

Purpose: Verifies CANCEL-01/02/03/04/05 against the real service (04-01) with integration tests mirroring the existing cashout test conventions.
Output: Three new `tests/cancel.*.test.js` files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-bet-cancellation-v2/04-RESEARCH.md
@.planning/phases/04-bet-cancellation-v2/04-01-PLAN.md

# The service under test (rewritten in 04-01)
@src/services/wagerService.js
# Seed helpers available to all cancel tests
@tests/helpers/testDb.js
# Structure/convention to mirror for the audit-trail test
@tests/cashout.audit.test.js
# Structure/convention to mirror for happy-path setup
@tests/cashout.cancel-refund.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: cancel.happy-path.test.js — clean cancellation refunds 95%, status 'refunded'</name>
  <files>tests/cancel.happy-path.test.js</files>
  <action>
Create tests/cancel.happy-path.test.js following the setup convention of tests/cashout.cancel-refund.test.js: import { applyBaseSchema, applyWalletSchema, applyCashoutMigration, seedTestUser, seedWallet, seedOpenMarket, seedWager, wait, closePool } from './helpers/testDb', import { query } from '../src/config/database', import wagerService from '../src/services/wagerService'. In beforeAll apply the three schemas and seed a test user; in afterAll closePool.

Tests covering CANCEL-01 and CANCEL-05:
- Seed a wallet (e.g. 1000), a binary open market, and a pending wager (amount 100, oddsAtTime 2, cashedOutAmount 0). Call `await wagerService.cancelWager(wagerId, userId)`, then `await wait()`.
- Assert the returned object reports `ok: true` and `refunded` = 95, `fee` = 5 (default CANCEL_FEE_PERCENT 5).
- Assert wallet balance delta = +95 (95% of the 100 stake), not 100 — this is the fee being applied (CANCEL-02 sanity at the happy-path level).
- Assert the wager row status is 'refunded' (CANCEL-05; the UI labels this "Cancelada").
- Assert one wallet_transactions row exists for this wager with type 'refund'.

Read CANCEL_FEE_PERCENT from `require('../src/config/env')` rather than hardcoding 5, so the test tracks the configured fee.
  </action>
  <verify>
    <automated>npx jest tests/cancel.happy-path.test.js 2>&1 | tail -20</automated>
    <human-check>No live *test*-named Postgres reachable in this sandbox (carried-forward blocker). If jest cannot connect, confirm pass against a real Postgres test DB or record the mock-backed dry-run compensation in the SUMMARY, per Phases 1-3 precedent.</human-check>
  </verify>
  <done>tests/cancel.happy-path.test.js asserts refunded 95 / fee 5, +95 wallet delta, status 'refunded', and one 'refund' transaction for a clean pending wager.</done>
</task>

<task type="auto">
  <name>Task 2: cancel.fee-computation.test.js — fee off remaining stake, matches money util</name>
  <files>tests/cancel.fee-computation.test.js</files>
  <action>
Create tests/cancel.fee-computation.test.js with the same imports/setup convention. Also import `money` from '../src/utils/money' and `env` from '../src/config/env'.

Tests covering CANCEL-02 and CANCEL-03:
- Fee split correctness: for a clean pending wager (amount 100, cashedOutAmount 0), assert the service's returned `fee`/`refunded` exactly equal `money.applyFeePercent(100, env.CANCEL_FEE_PERCENT)` `{ fee, net }` — never a raw `100 * 0.95` float. Use a couple of amounts (e.g. 100 and 33.33 if the schema allows decimals) to exercise the cents-based rounding, comparing against money.applyFeePercent output.
- CANCEL-03 defensive-formula documentation: seed a wager whose amount is known and assert the fee is computed off the remaining stake `amount - cashed_out_amount`. Per locked decision D-01 a wager with cashed_out_amount > 0 is BLOCKED (that case belongs to 04-03's blocking test), so here use cashedOutAmount 0 and assert remainingStake === amount drives the fee — i.e. the fee base equals the full stake when no cashout occurred. Add a comment noting the post-cashout branch is unreachable by design (D-01) and is proven blocked in cancel.blocking.test.js.

Read the fee percent from env, not a literal.
  </action>
  <verify>
    <automated>npx jest tests/cancel.fee-computation.test.js 2>&1 | tail -20</automated>
    <human-check>Test-DB blocker as above — compensate + document if jest cannot connect.</human-check>
  </verify>
  <done>tests/cancel.fee-computation.test.js asserts the service fee/net equal money.applyFeePercent(remainingStake, env.CANCEL_FEE_PERCENT) and that the fee base is the full stake when cashed_out_amount is 0.</done>
</task>

<task type="auto">
  <name>Task 3: cancel.audit.test.js — exactly one reconciling wallet_transactions row</name>
  <files>tests/cancel.audit.test.js</files>
  <action>
Create tests/cancel.audit.test.js mirroring tests/cashout.audit.test.js's structure and reconciliation style, adapted for cancellation (CANCEL-04).

Tests:
- Seed wallet (known balance, e.g. 500), open market, pending wager (amount 100, cashedOutAmount 0). Cancel it, then `await wait()`.
- Assert exactly one wallet_transactions row exists with related_entity 'wager' and related_id = the wager id (`toHaveLength(1)`).
- Assert that row: type 'refund', Number(amount) === the returned net refund (95), balance_before === 500, balance_after === 500 + net.
- Assert the real wallet balance equals balance_before + amount — proving the balance changed ONLY through the recorded movement (no raw UPDATE wallets bypassing the repository), the same guarantee cashout.audit.test.js proves for cashouts.
- Assert the description is non-empty and documents the fee (so the audit row is human-readable per CANCEL-04) — a substring check for the fee percent or "taxa" is sufficient without over-coupling to exact wording.

Read the fee percent from env where needed.
  </action>
  <verify>
    <automated>npx jest tests/cancel.audit.test.js 2>&1 | tail -20</automated>
    <human-check>Test-DB blocker as above — compensate + document if jest cannot connect.</human-check>
  </verify>
  <done>tests/cancel.audit.test.js asserts exactly one reconciling 'refund' wallet_transactions row per cancellation whose amount/balance_before/balance_after match the real wallet, proving the balance moved only via the recorded movement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> real service/DB | Tests drive the real, unmodified cancelWager; they must not stub the money math or the wallet writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Repudiation / financial integrity | audit-row reconciliation | high | mitigate | cancel.audit.test.js proves the wallet balance changed only through the recorded 'refund' movement (no bypass), reconciling balance_before/after with the real balance |
| T-04-07 | Tampering (silent float drift) | fee computation | medium | mitigate | cancel.fee-computation.test.js pins the service output to money.applyFeePercent, catching any hand-rolled float regression |
| T-04-SC | Tampering (supply chain) | dependencies | low | accept | No new packages; tests use existing jest + testDb helper |
</threat_model>

<verification>
- `npx jest tests/cancel.happy-path.test.js tests/cancel.fee-computation.test.js tests/cancel.audit.test.js` (subject to the carried-forward test-DB blocker; compensate with a mock-backed dry run and document per prior-phase precedent).
- Tests read CANCEL_FEE_PERCENT from env, not hardcoded, so they track config.
</verification>

<success_criteria>
Positive-path cancellation behavior (CANCEL-01/02/03/04/05) is proven by integration tests against the real cancelWager: 95% net refund, 5% fee equal to money.applyFeePercent, status 'refunded', and exactly one reconciling audit row.
</success_criteria>

<output>
Create `.planning/phases/04-bet-cancellation-v2/04-02-SUMMARY.md` when done.
</output>
