---
phase: 02-partial-cashout
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/repositories/cashoutRepository.js
  - src/repositories/wagerRepository.js
  - src/repositories/marketRepository.js
autonomous: true
requirements: [CASHOUT-06, CASHOUT-07]
must_haves:
  truths:
    - "A cashout row can be inserted transactionally and a duplicate (wager_id, idempotency_key) surfaces a catchable 23505 to the caller"
    - "The existing prior-committed cashout for a given idempotency key can be read back"
    - "A wager and a market can each be row-locked by id (FOR UPDATE) inside a transaction, with wager ownership baked into the WHERE clause"
  artifacts:
    - "src/repositories/cashoutRepository.js (create + findByIdempotencyKey, both client-scoped)"
    - "src/repositories/wagerRepository.js findByIdForUpdate(id, marketId, userId, client)"
    - "src/repositories/marketRepository.js findByIdForUpdate(id, client)"
  key_links:
    - "cashoutRepository.create lets 23505 bubble (service catches it) — matches where notificationService, not notificationRepository, does the catch"
    - "wagerRepository.findByIdForUpdate bakes user_id into WHERE = IDOR-safe lock (CASHOUT-06 / access control)"
    - "marketRepository.findByIdForUpdate DRYs the inlined lock SQL duplicated at 4 existing call sites"
---

<objective>
Add the data-access primitives the cashout service needs: a new `cashoutRepository` (idempotent insert + read-back) and `findByIdForUpdate` lock helpers on the wager and market repositories, following the exact client-param + FOR UPDATE conventions already proven in this codebase.

Purpose: Isolates all new SQL behind the repository layer (per CLAUDE.md layering) so the service plan (02-03) is pure orchestration. The lock helpers enforce the market→wager→wallet ordering and IDOR-safe ownership scoping the whole concurrency story rests on.
Output: One new repository + two additive lock methods.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-partial-cashout/02-RESEARCH.md
@.planning/phases/02-partial-cashout/02-PATTERNS.md

# Analogs (copy shape exactly):
@src/repositories/walletRepository.js
@src/repositories/notificationRepository.js
@src/repositories/wagerRepository.js
@src/repositories/marketRepository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/repositories/cashoutRepository.js</name>
  <files>src/repositories/cashoutRepository.js</files>
  <action>Create src/repositories/cashoutRepository.js as a class exporting a singleton instance (module.exports = new CashoutRepository()), matching every repository in the codebase. Add two methods, both taking the transactional `client` as their last param and always calling client.query (never the bare `query` import — this runs inside wagerService.cashoutWager's transaction, per walletRepository.recordTransaction's signature precedent):
  1. create({ wagerId, userId, stakeCashedOut, grossValue, feeAmount, netValue, idempotencyKey }, client): INSERT INTO wager_cashouts (wager_id, user_id, stake_cashed_out, gross_value, fee_amount, net_value, idempotency_key) VALUES ($1..$7) RETURNING *; return result.rows[0]. Do NOT catch the 23505 here — let it bubble so the service decides (this mirrors notificationRepository.create, where the SERVICE not the repository owns the 23505 semantics; see 02-PATTERNS.md analog 1). Add a JSDoc note that a duplicate (wager_id, idempotency_key) throws err.code === '23505' for the caller to handle.
  2. findByIdempotencyKey(wagerId, idempotencyKey, client): plain SELECT * FROM wager_cashouts WHERE wager_id = $1 AND idempotency_key = $2; return result.rows[0] || null. This is the read-back the service uses to replay a prior committed result on a retried request (CASHOUT-07).
  Follow the project's Portuguese JSDoc convention and parameterized-query rule.</action>
  <verify>
    <automated>node --check src/repositories/cashoutRepository.js && node -e "const r=require('./src/repositories/cashoutRepository');if(typeof r.create!=='function'||typeof r.findByIdempotencyKey!=='function')throw new Error('methods');const src=require('fs').readFileSync('src/repositories/cashoutRepository.js','utf8');if(/catch\s*\(/.test(src))throw new Error('repo must not catch 23505 — service owns it');if(!/RETURNING \*/.test(src))throw new Error('create must RETURNING *');console.log('OK')"</automated>
  </verify>
  <done>cashoutRepository exports create (RETURNING *, no 23505 catch) and findByIdempotencyKey, both client-scoped; singleton export.</done>
</task>

<task type="auto">
  <name>Task 2: Add findByIdForUpdate lock helpers to wager and market repositories</name>
  <files>src/repositories/wagerRepository.js, src/repositories/marketRepository.js</files>
  <action>Additive changes only — do not alter existing methods.
  In src/repositories/wagerRepository.js add findByIdForUpdate(id, marketId, userId, client): SELECT * FROM wagers WHERE id = $1 AND market_id = $2 AND user_id = $3 FOR UPDATE; return result.rows[0] || null. Mirror the existing findPendingByMarket shape (client.query + FOR UPDATE) but by id, with market_id and user_id baked into the WHERE clause — this is the IDOR-safe lock query from RESEARCH Pattern 1 (ownership enforced in the lock itself, stronger than cancelWager's post-lock check).
  In src/repositories/marketRepository.js add findByIdForUpdate(id, client): SELECT * FROM markets WHERE id = $1 FOR UPDATE; return result.rows[0] || null. This DRYs the `SELECT * FROM markets WHERE id = $1 FOR UPDATE` currently inlined at 4 call sites (requisitos.txt: no duplicated code). Match the sibling `resolve(id, outcome, client)` method's client-param shape. Do NOT retrofit the existing 4 call sites in this plan — that is the service plan's concern; here we only add the reusable method.</action>
  <verify>
    <automated>node --check src/repositories/wagerRepository.js && node --check src/repositories/marketRepository.js && node -e "const w=require('./src/repositories/wagerRepository');const m=require('./src/repositories/marketRepository');if(typeof w.findByIdForUpdate!=='function')throw new Error('wager helper');if(typeof m.findByIdForUpdate!=='function')throw new Error('market helper');const ws=require('fs').readFileSync('src/repositories/wagerRepository.js','utf8');if(!/WHERE id = \$1 AND market_id = \$2 AND user_id = \$3 FOR UPDATE/.test(ws))throw new Error('wager lock must bake ownership into WHERE');console.log('OK')"</automated>
  </verify>
  <done>wagerRepository.findByIdForUpdate locks by id with market_id + user_id ownership in WHERE; marketRepository.findByIdForUpdate locks a market by id; both use client.query + FOR UPDATE; existing methods untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → repository | untrusted wagerId/userId flow into parameterized lock queries |
| repository → DB | row locks + unique constraint enforce serialization and replay-safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Elevation of Privilege | wagerRepository.findByIdForUpdate | high | mitigate | Ownership (user_id = $3) baked into the FOR UPDATE lock query so a caller can never lock/read another user's wager by guessing an id (IDOR) — asserted by the WHERE-clause grep in Task 2 |
| T-02-02-02 | Repudiation | cashoutRepository idempotency | high | mitigate | create() surfaces 23505 on duplicate (wager_id, idempotency_key); findByIdempotencyKey replays the prior committed row — the two halves of CASHOUT-07's replay defense |
| T-02-02-03 | Tampering | SQL injection | medium | mitigate | All new queries are parameterized ($1..$n); no string interpolation (CLAUDE.md DB rule) |
</threat_model>

<verification>
- Syntax + method presence for all three repositories.
- wager lock query bakes ownership into WHERE (IDOR).
- cashoutRepository.create does not swallow 23505.
</verification>

<success_criteria>
The data layer exposes idempotent cashout persistence and IDOR-safe, correctly-ordered row locks; no existing repository behavior changed.
</success_criteria>

<output>
Create `.planning/phases/02-partial-cashout/02-02-SUMMARY.md` when done
</output>
