---
phase: 02-partial-cashout
plan: 07
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - tests/cashout.concurrency.test.js
  - tests/cashout.idempotency.test.js
  - tests/cashout.audit.test.js
autonomous: true
requirements: [CASHOUT-06, CASHOUT-07, CASHOUT-08]
must_haves:
  truths:
    - "Two genuinely concurrent cashout requests on the same wager (fired via Promise.all, not sequential awaits) can never both succeed if their combined stake would exceed the wager's remaining balance"
    - "A retried request with the same idempotency key never double-applies the wallet credit or the cashed_out_amount increment, even when fired concurrently with the original"
    - "Every successful cashout produces exactly one wallet_transactions row with type='credit', related_entity='cashout', and an amount matching the wallet balance delta exactly"
  artifacts:
    - "tests/cashout.concurrency.test.js"
    - "tests/cashout.idempotency.test.js"
    - "tests/cashout.audit.test.js"
  key_links:
    - "These tests are the functional proof of Plan 02-04's lock ordering (CASHOUT-06) and idempotency design (CASHOUT-07) and Plan 02-04's audit-trail write (CASHOUT-08) — they drive the real, unmodified wagerService.cashoutWager"
---

<objective>
Prove, with real concurrent-execution tests, the three attack vectors requisitos.txt explicitly mandates for this phase: two simultaneous cashout requests on the same wager (race/double-spend), a replayed request with the same idempotency key, and the completeness of the audit trail (wallet_transactions + balance-delta match) for every successful cashout.

Purpose: RESEARCH.md's lock-ordering and idempotency design (Plan 02-04) is only as good as its proof under real concurrent load. This plan exists specifically to fire genuinely simultaneous requests via `Promise.all` (not sequential awaits, which would never actually exercise row-lock contention) against the real, committed `wagerService.cashoutWager`.
Output: Three test files proving CASHOUT-06 (concurrency), CASHOUT-07 (idempotent replay), and CASHOUT-08 (audit trail) against real, unmodified production code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-partial-cashout/02-RESEARCH.md
@.planning/phases/02-partial-cashout/02-PATTERNS.md

# Test setup precedent (read in full before writing):
@tests/notifications.emission.test.js
@tests/helpers/testDb.js
@src/services/wagerService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write cashout.concurrency.test.js — two simultaneous cashouts on the same wager</name>
  <files>tests/cashout.concurrency.test.js</files>
  <action>Following the exact setup precedent of tests/notifications.emission.test.js (assertTestDatabase-guarded beforeAll applying applyBaseSchema/applyWalletSchema/applyCashoutMigration, seedTestUser, seedWallet, seedOpenMarket, per-test cleanup, afterAll closePool), seed a single pending wager with amount=100, odds_at_time=2, cashed_out_amount=0. Fire two DIFFERENT-idempotency-key cashout requests concurrently via `Promise.allSettled([wagerService.cashoutWager(wagerId, userId, { amount: 60, idempotencyKey: 'key-a' }), wagerService.cashoutWager(wagerId, userId, { amount: 60, idempotencyKey: 'key-b' })])` — both request 60 against a remaining stake of 100, so at most one can succeed (60+60=120 > 100 remaining). Assert exactly one of the two settled results is fulfilled and the other is rejected with a ValidationError or ConflictError (whichever the second transaction's re-validated state produces after the first commits). Assert the wager's final cashed_out_amount in the DB is exactly 60 (not 120, not 0), and the wallet's balance reflects exactly one successful credit. Also add a second scenario using `Promise.all` with two requests that CAN both legitimately succeed (e.g., amount=20 each against remaining=100) to prove the lock serializes them correctly in sequence rather than corrupting state — assert final cashed_out_amount is exactly 40 and the wallet was credited twice, not once (proving the lock doesn't over-serialize into data loss either). If a live *test*-named Postgres database is unreachable in this sandbox (carried-forward Phase 1 limitation, see STATE.md), compensate exactly as Phase 1 did: write this test to the real interface contract and additionally run a temporary mock-backed jest dry run (deleted before commit) simulating lock contention via a fake client that serializes concurrent FOR UPDATE calls, documenting the result in the plan SUMMARY per Plan 01-04's precedent.</action>
  <verify>
    <automated>npx jest tests/cashout.concurrency.test.js</automated>
  </verify>
  <done>tests/cashout.concurrency.test.js fires genuinely simultaneous cashout requests via Promise.all/allSettled and proves the row lock serializes them correctly — over-subscribed concurrent requests never both succeed, and under-subscribed concurrent requests both succeed without data loss.</done>
</task>

<task type="auto">
  <name>Task 2: Write cashout.idempotency.test.js — replayed idempotency key never double-applies</name>
  <files>tests/cashout.idempotency.test.js</files>
  <action>Same setup precedent as Task 1. Seed a pending wager (amount=100, odds_at_time=2). Case 1 (sequential retry): call `wagerService.cashoutWager(wagerId, userId, { amount: 30, idempotencyKey: 'retry-key' })`, await it, then call it again with the identical arguments — assert both calls return the same cashout id and identical netValue, assert the wager's cashed_out_amount is 30 (not 60), and assert exactly one wallet_transactions row with related_entity='cashout' exists for this wager (not two). Case 2 (concurrent retry — the harder case): fire the exact same call twice via `Promise.all` with the identical idempotencyKey — assert both promises resolve to the same cashout id/netValue (one hits the real insert path, the other catches 23505 and replays), the wager's cashed_out_amount is still exactly 30, and the wallet was credited exactly once. If a live *test*-named Postgres database is unreachable in this sandbox, compensate exactly as Phase 1 did (temporary mock-backed jest dry run, deleted before commit, documented in SUMMARY).</action>
  <verify>
    <automated>npx jest tests/cashout.idempotency.test.js</automated>
  </verify>
  <done>tests/cashout.idempotency.test.js proves both a sequential retry and a genuinely concurrent retry with the same idempotency key never double-apply the wallet credit or the cashed_out_amount increment.</done>
</task>

<task type="auto">
  <name>Task 3: Write cashout.audit.test.js — wallet_transactions row and balance-delta match</name>
  <files>tests/cashout.audit.test.js</files>
  <action>Same setup precedent as Task 1. Seed a pending wager and a funded wallet with a known starting balance. Call `wagerService.cashoutWager` with a valid amount, then assert: (1) exactly one new `wallet_transactions` row exists with `type='credit'`, `related_entity='cashout'`, `related_id` equal to the returned cashout's id, and `amount` equal to the returned `netValue`; (2) the row's `balance_before`/`balance_after` exactly bracket the known starting balance and the credited netValue (no drift); (3) the wallet's actual balance column equals `balance_before` from step 2 plus `netValue`, proving the balance was only ever changed via the recorded movement, never a bare UPDATE bypassing `recordTransaction`. Add a static source-level check (grep, in this task's verify block, not the action) confirming `walletRepository.adjustBalance` and `walletRepository.recordTransaction` are the only two call sites in `wagerService.js`'s `cashoutWager` region that touch wallet state — i.e., no raw `UPDATE wallets` string appears inside that method's source region. If a live *test*-named Postgres database is unreachable in this sandbox, compensate exactly as Phase 1 did (temporary mock-backed jest dry run, deleted before commit, documented in SUMMARY).</action>
  <verify>
    <automated>npx jest tests/cashout.audit.test.js && node -e "const src=require('fs').readFileSync('src/services/wagerService.js','utf8');const start=src.indexOf('async cashoutWager(');const rest=src.slice(start);const nextRel=rest.indexOf('\n  async ',10);const region=nextRel===-1?rest:rest.slice(0,nextRel);if(/UPDATE\s+wallets/i.test(region))throw new Error('cashoutWager must not raw-UPDATE wallets directly — use walletRepository.adjustBalance');console.log('OK')"</automated>
  </verify>
  <done>tests/cashout.audit.test.js proves every successful cashout produces exactly one matching wallet_transactions row whose amount and balance_before/after exactly reconcile with the wallet's actual balance change; static grep confirms no raw wallet UPDATE bypasses the repository inside cashoutWager.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent HTTP requests → single wager row | Two or more in-flight requests racing against the same financial state |
| retried/duplicated HTTP request → same wager row | A client or network-level retry re-sending an already-processed request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Tampering | double-spend via concurrent cashout race | high | mitigate | Proven functionally: over-subscribed concurrent requests never both succeed (Task 1); this is the required requisitos.txt concurrency test for this phase |
| T-02-07-02 | Repudiation | replay via retried idempotency key | high | mitigate | Proven functionally for both sequential and genuinely concurrent replay (Task 2) |
| T-02-07-03 | Repudiation | missing or mismatched audit trail | high | mitigate | Proven functionally: every successful cashout produces exactly one wallet_transactions row whose amount reconciles exactly with the balance delta (Task 3); static grep confirms no raw UPDATE bypasses the repository |
</threat_model>

<verification>
- `npx jest tests/cashout.concurrency.test.js tests/cashout.idempotency.test.js tests/cashout.audit.test.js` green (or compensated per documented sandbox limitation).
- Static grep confirms cashoutWager never issues a raw wallet UPDATE outside walletRepository.
</verification>

<success_criteria>
The three attack vectors requisitos.txt mandates (concurrent race, replayed idempotency key, and audit-trail completeness) are proven against the real, unmodified wagerService.cashoutWager.
</success_criteria>

<output>
Create `.planning/phases/02-partial-cashout/02-07-SUMMARY.md` when done
</output>
