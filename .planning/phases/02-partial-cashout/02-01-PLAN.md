---
phase: 02-partial-cashout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/money.js
  - tests/money.test.js
  - src/migrations/004_cashout.js
  - src/config/env.js
  - tests/helpers/testDb.js
autonomous: true
requirements: [CASHOUT-10, CASHOUT-03, CASHOUT-07]
must_haves:
  truths:
    - "A shared money utility computes stake × odds and applies a fee percentage with no float drift across repeated operations"
    - "The database has a place to record each cashout row and to track cumulative cashed-out stake per wager"
    - "A second cashout row with the same (wager_id, idempotency_key) is rejected by the database"
  artifacts:
    - "src/utils/money.js (toCents, fromCents, multiply, subtract, applyFeePercent)"
    - "tests/money.test.js (passing, runs live in sandbox)"
    - "src/migrations/004_cashout.js (wager_cashouts table + wagers.cashed_out_amount column)"
    - "src/config/env.js CASHOUT_FEE_PERCENT (default 0)"
    - "tests/helpers/testDb.js applyCashoutMigration / seedOpenMarket / seedWager"
  key_links:
    - "money.js precision (NUMERIC(15,2) integer-cents) matches every money column in the schema"
    - "wager_cashouts UNIQUE(wager_id, idempotency_key) is the idempotency backbone for CASHOUT-07"
    - "wagers.cashed_out_amount DEFAULT 0 makes the resolveMarket remaining-stake edit reduce to current behavior when no cashout occurred"
---

<objective>
Establish the foundation for partial cashout: a shared decimal-safe money utility (CASHOUT-10), the schema (append-only `wager_cashouts` table + `wagers.cashed_out_amount` column) that all later plans read/write, a configurable-but-zero cashout fee, and the test-DB scaffolding every later cashout test needs.

Purpose: Every downstream plan (repositories, service, resolveMarket edit, tests) depends on these primitives existing. Getting the money math and schema right here prevents float-drift and idempotency bugs later.
Output: `src/utils/money.js` (+ passing unit test), `src/migrations/004_cashout.js`, `CASHOUT_FEE_PERCENT` in env, extended `tests/helpers/testDb.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-partial-cashout/02-RESEARCH.md
@.planning/phases/02-partial-cashout/02-PATTERNS.md

# Analogs to copy structure from:
@src/migrations/002_wallet.js
@src/migrations/003_notifications.js
@src/config/env.js
@tests/helpers/testDb.js
@src/utils/errors.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/utils/money.js decimal-safe utility with unit tests</name>
  <files>src/utils/money.js, tests/money.test.js</files>
  <behavior>
    - toCents(1.005) rounds to 101 (integer cents), fromCents(101) === 1.01
    - multiply(10, 2.5) === 25 (stake × odds); multiply(33.33, 3) has no float tail (=== 99.99)
    - subtract(100, 33.33) === 66.67 with no float artifact
    - applyFeePercent(100, 0) returns { fee: 0, net: 100 } (fee term present but zero — BR-1)
    - applyFeePercent(100, 5) returns { fee: 5, net: 95 } (proves the fee term is wired for a future nonzero fee)
    - Repeated multiply/subtract over 20 iterations accumulates zero drift (CASHOUT-10: the anti-drift guarantee)
  </behavior>
  <action>Create src/utils/money.js exactly per the RESEARCH.md "Code Examples" spec (toCents, fromCents, multiply, subtract, applyFeePercent) using integer-cents arithmetic — zero new dependencies. Export a plain named-functions object (module.exports = { ... }), matching src/utils/errors.js's export shape, NOT a class singleton (money has no state). This is the single shared money-math utility CASHOUT-10 mandates; it replaces the scattered inline `Math.round(x * 100) / 100` pattern. Keep applyFeePercent(amount, feePercent) returning both fee and net so a future milestone can set a nonzero fee without a formula rewrite (BR-1: fee term stays, value is 0 this milestone). Then write tests/money.test.js covering the behaviors above. This test runs live in the sandbox (pure functions, no DB) and MUST pass — it is the real green signal for CASHOUT-10.</action>
  <verify>
    <automated>npx jest tests/money.test.js</automated>
  </verify>
  <done>tests/money.test.js passes live; money.js exports the five functions with integer-cents precision and a fee term that computes 0 at feePercent=0 and a correct nonzero fee otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Create migration 004_cashout.js and add CASHOUT_FEE_PERCENT to env</name>
  <files>src/migrations/004_cashout.js, src/config/env.js</files>
  <action>Create src/migrations/004_cashout.js following the exact module shape of 003_notifications.js (const migrations = [...]; module.exports = { id: '004_cashout', up: migrations, down: [...] }). The `up` array creates the `wager_cashouts` table per 02-PATTERNS.md: columns id SERIAL PK, wager_id INTEGER NOT NULL REFERENCES wagers(id) ON DELETE CASCADE, user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE, stake_cashed_out NUMERIC(15,2) NOT NULL CHECK (stake_cashed_out > 0), gross_value NUMERIC(15,2) NOT NULL, fee_amount NUMERIC(15,2) NOT NULL, net_value NUMERIC(15,2) NOT NULL, idempotency_key VARCHAR(200) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, and UNIQUE (wager_id, idempotency_key). Add CREATE INDEX IF NOT EXISTS idx_wager_cashouts_wager_id ON wager_cashouts(wager_id). Then, as a separate statement in the same `up` array, ALTER TABLE wagers ADD COLUMN IF NOT EXISTS cashed_out_amount NUMERIC(15,2) NOT NULL DEFAULT 0. Use NUMERIC(15,2) to match every existing money column — do NOT invent a different precision. The `down` array mirrors in reverse order: drop the wagers column first, then DROP TABLE IF EXISTS wager_cashouts. Do NOT touch the wallet_transactions.type CHECK constraint (RESEARCH Pitfall 4 — cashout reuses type 'credit'). In src/config/env.js add CASHOUT_FEE_PERCENT: parseFloat(process.env.CASHOUT_FEE_PERCENT || '0') following the existing SESSION_TIMEOUT/BCRYPT_ROUNDS placement and comment style; default 0 (BR-1). Do NOT add a CASHOUT_MIN_AMOUNT env var — BR-2 mandates no minimum floor beyond the positive-amount check enforced later in the service. If a .env.example file exists at repo root, add a documented CASHOUT_FEE_PERCENT=0 line to it as well.</action>
  <verify>
    <automated>node --check src/migrations/004_cashout.js && node --check src/config/env.js && node -e "const m=require('./src/migrations/004_cashout');const up=m.up.join(' ');if(m.id!=='004_cashout')throw new Error('id');if(!/UNIQUE\s*\(wager_id,\s*idempotency_key\)/.test(up))throw new Error('missing idempotency unique');if(!/cashed_out_amount/.test(up))throw new Error('missing column');if(!/NUMERIC\(15,\s*2\)/.test(up))throw new Error('precision');if(m.down.length!==2)throw new Error('down');const e=require('./src/config/env');if(e.CASHOUT_FEE_PERCENT!==0)throw new Error('fee default not 0');console.log('OK')"</automated>
  </verify>
  <done>Migration 004 defines wager_cashouts with the (wager_id, idempotency_key) UNIQUE constraint, adds wagers.cashed_out_amount DEFAULT 0, has a mirrored 2-step down; env.CASHOUT_FEE_PERCENT resolves to 0; no CASHOUT_MIN_AMOUNT introduced; wallet_transactions CHECK untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/helpers/testDb.js with cashout migration + wager/market seeders</name>
  <files>tests/helpers/testDb.js</files>
  <action>Extend tests/helpers/testDb.js (additive only — keep every existing export) following the exact precedent of applyWalletSchema()/seedWallet() (Phase 1 Plan 04). Add applyCashoutMigration() that lazy-requires src/migrations/004_cashout and runs each string in its `up` array via query(), guarded by assertTestDatabase() (same shape as applyNotificationsMigration). Add seedOpenMarket({ oddsYes, oddsNo }) that inserts an open market (status 'open') into markets and returns its id (parameterized INSERT; reuse the columns marketRepository.create writes). Add seedWager({ userId, marketId, choice, amount, oddsAtTime, potentialPayout, cashedOutAmount }) that inserts a pending wager row (status 'pending') with a cashed_out_amount column value (default 0) and returns the wager id — this lets the resolution-integration test (Plan 04) seed a wager with prior cashout WITHOUT depending on the cashout write path. Every new function calls assertTestDatabase() first. Add the new functions to module.exports.</action>
  <verify>
    <automated>node --check tests/helpers/testDb.js && node -e "const t=require('./tests/helpers/testDb');['applyCashoutMigration','seedOpenMarket','seedWager','applyWalletSchema','seedWallet'].forEach(f=>{if(typeof t[f]!=='function')throw new Error('missing '+f)});console.log('OK')"</automated>
  </verify>
  <done>testDb.js exports applyCashoutMigration, seedOpenMarket, seedWager (all assertTestDatabase-guarded) alongside the existing helpers; syntax valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | DDL defines the constraints all later financial writes rely on |
| config env → runtime | fee percentage read from environment feeds the payout formula |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering | wager_cashouts idempotency | high | mitigate | UNIQUE(wager_id, idempotency_key) at the DB level is the enforcement point for replay protection (CASHOUT-07); a missing/misspelled constraint would let later replay defenses silently no-op — verified by the constraint grep in Task 2 |
| T-02-01-02 | Information Disclosure | fee misconfig | low | accept | CASHOUT_FEE_PERCENT defaults to 0 and is server-read only; never client-supplied. Wrong value is an operational/config concern, not an attack surface |
| T-02-01-SC | Tampering | dependency supply chain | low | accept | No new npm package is installed — money.js is a zero-dependency integer-cents utility (RESEARCH recommendation). No install checkpoint required this phase |
</threat_model>

<verification>
- `npx jest tests/money.test.js` green (CASHOUT-10 real signal).
- Migration 004 structurally validated (id, idempotency UNIQUE, cashed_out_amount, NUMERIC(15,2), mirrored down).
- env.CASHOUT_FEE_PERCENT === 0; no CASHOUT_MIN_AMOUNT added.
- testDb.js exposes the three new helpers.
</verification>

<success_criteria>
money.js proves zero float drift across repeated operations; the schema and config foundation exists for every downstream cashout plan; no new runtime dependency added.
</success_criteria>

<output>
Create `.planning/phases/02-partial-cashout/02-01-SUMMARY.md` when done
</output>
