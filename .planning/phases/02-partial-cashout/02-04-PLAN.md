---
phase: 02-partial-cashout
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/services/wagerService.js
  - src/repositories/wagerRepository.js
  - tests/cashout.computation.test.js
  - tests/cashout.validation.test.js
autonomous: true
requirements: [CASHOUT-01, CASHOUT-02, CASHOUT-04, CASHOUT-05, CASHOUT-06, CASHOUT-07, CASHOUT-09, CASHOUT-10]
must_haves:
  truths:
    - "A user can request a cashout on part of their own open, pending wager and receive a value computed entirely server-side from stake × odds_at_time, minus fee — a client-submitted value is never used"
    - "A cashout is rejected once the market is no longer open, or the wager is no longer pending"
    - "A cashout can never reduce a wager's remaining stake to zero or below (partial-only this milestone)"
    - "A retried request with the same idempotency key returns the original committed result without re-applying the wallet credit or the wagers.cashed_out_amount increment"
    - "Cashout logic never reads or branches on the wager's binary yes/no selection field (market-type-agnostic per CASHOUT-09)"
  artifacts:
    - "src/services/wagerService.js cashoutWager(wagerId, userId, { amount, idempotencyKey })"
    - "src/repositories/wagerRepository.js incrementCashedOutAmount(id, stake, client)"
    - "tests/cashout.computation.test.js"
    - "tests/cashout.validation.test.js"
  key_links:
    - "market→wager→wallet FOR UPDATE lock ordering (marketRepository.findByIdForUpdate → wagerRepository.findByIdForUpdate → walletRepository.findByUserIdForUpdate, all from Plan 02-02/existing) is what makes concurrent cashouts safe (CASHOUT-06)"
    - "cashoutRepository.create + UNIQUE(wager_id, idempotency_key) + 23505-catch-and-replay (Plan 02-02) is what makes retries idempotent (CASHOUT-07)"
    - "domainEvents.emit('wager.cashed_out', ...) fires strictly after transaction() resolves — never inside the closure (D-01), matched by Plan 02-05's listener"
---

<objective>
Implement `wagerService.cashoutWager(wagerId, userId, { amount, idempotencyKey })` — the core financial transaction of this phase: validates input, locks market → wager → wallet in that fixed order inside one `transaction()`, re-validates all business invariants after locks are held, computes the stake-proportional cashout value via `money.js`, persists it idempotently, credits the wallet with a matching audit record, and emits `wager.cashed_out` strictly after commit.

Purpose: This is the phase's single highest-risk piece of code — it is where the deadlock-ordering finding, the idempotency design, and the money formula all converge. Isolating it in its own plan (single file, single method) keeps the executor's context focused entirely on getting this transaction correct.
Output: `cashoutWager()` implemented per RESEARCH.md's exact worked pattern; `wagerRepository.incrementCashedOutAmount` helper; computation and validation test suites proving the formula, the market-type-agnostic guarantee, and every rejection path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-partial-cashout/02-RESEARCH.md
@.planning/phases/02-partial-cashout/02-PATTERNS.md

# Files being edited/extended (read in full before editing):
@src/services/wagerService.js
@src/repositories/wagerRepository.js
@src/repositories/marketRepository.js
@src/repositories/cashoutRepository.js
@src/utils/money.js
@src/config/env.js
@tests/helpers/testDb.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement wagerService.cashoutWager and wagerRepository.incrementCashedOutAmount</name>
  <files>src/services/wagerService.js, src/repositories/wagerRepository.js</files>
  <behavior>
    - Given a pending wager (amount=100, odds_at_time=2, cashed_out_amount=0) and CASHOUT_FEE_PERCENT=0, requesting amount=30 with a fresh idempotency key returns net=60, gross=60, fee=0, and leaves cashed_out_amount=30
    - Requesting amount<=0 throws ValidationError before any lock is acquired
    - Requesting on a wager whose market.status !== 'open' throws ConflictError; requesting on a wager whose status !== 'pending' throws ConflictError
    - Requesting an amount >= remainingStake (amount - cashed_out_amount) throws ValidationError (never allows remaining stake to reach zero or below)
    - Requesting a valid amount does not emit domainEvents.emit('wager.cashed_out', ...) if the transaction throws (rollback emits nothing, per D-01)
    - Retrying the exact same (wagerId, idempotencyKey) after a first success returns the identical cashout result and does not increment cashed_out_amount or credit the wallet a second time
  </behavior>
  <action>In src/repositories/wagerRepository.js add `incrementCashedOutAmount(id, stake, client)`: `UPDATE wagers SET cashed_out_amount = cashed_out_amount + $1 WHERE id = $2 RETURNING *;` via `client.query`, matching the existing `updateStatus` method's client-param shape (additive, do not touch existing methods).

In src/services/wagerService.js add requires: `const marketRepository = require('../repositories/marketRepository');`, `const cashoutRepository = require('../repositories/cashoutRepository');`, `const money = require('../utils/money');`, `const env = require('../config/env');`. Add `async cashoutWager(wagerId, userId, { amount, idempotencyKey })` to the `WagerService` class, following RESEARCH.md's Architecture Patterns Pattern 1 + Pattern 2 exactly:
1. Validate `requestedStake = Number(amount)` is finite and > 0 (ValidationError 'Valor do cashout precisa ser maior que zero.') — this is the full extent of CASHOUT-04's minimum-amount check per the locked business rule (no additional floor). Validate `idempotencyKey` is a non-empty string (ValidationError).
2. Inside `transaction(async (client) => { ... })`: peek `SELECT market_id FROM wagers WHERE id = $1;` (unlocked, just to find which market to lock) — NotFoundError if no row. Lock the market via `marketRepository.findByIdForUpdate(marketId, client)` (LOCK ORDER: market first) — ConflictError if missing or `market.status !== 'open'`. Lock the wager via `wagerRepository.findByIdForUpdate(wagerId, market.id, userId, client)` (LOCK ORDER: wager second; ownership+market baked into the query per CASHOUT-06/IDOR) — NotFoundError if missing, ConflictError if `wager.status !== 'pending'`.
3. Compute `remainingStake = Number(wager.amount) - Number(wager.cashed_out_amount)`; if `requestedStake >= remainingStake`, throw ValidationError ('Valor excede o saldo apostável restante para cashout parcial.') — strictly `>=`, never allowing the remaining stake to reach exactly zero (full cashout is v2-only).
4. Compute `gross = money.multiply(requestedStake, Number(wager.odds_at_time))`; `const { fee, net } = money.applyFeePercent(gross, env.CASHOUT_FEE_PERCENT)` (fee resolves to 0 this milestone per the locked business rule, but the term stays wired for a future nonzero fee).
5. Attempt `cashoutRepository.create({ wagerId, userId, stakeCashedOut: requestedStake, grossValue: gross, feeAmount: fee, netValue: net, idempotencyKey }, client)`. Wrap in try/catch: on `err.code === '23505'`, call `cashoutRepository.findByIdempotencyKey(wagerId, idempotencyKey, client)` and return that existing row immediately (do NOT proceed to steps 6-8 — the whole point is that a duplicate never reaches the mutating statements). On any other error, rethrow.
6. If the create succeeded (not a replay): `wagerRepository.incrementCashedOutAmount(wagerId, requestedStake, client)` (LOCK ORDER unaffected — same locked wager row).
7. `walletRepository.findByUserIdForUpdate(userId, client)` (LOCK ORDER: wallet third); `walletRepository.adjustBalance(wallet.id, net, client)`; `walletRepository.recordTransaction({ walletId: wallet.id, type: 'credit', amount: net, balanceBefore: wallet.balance, balanceAfter: updated.balance, relatedEntity: 'cashout', relatedId: cashout.id, description: \`Cashout parcial da aposta #${wagerId}\` }, client)` — CASHOUT-08, reusing `type: 'credit'` (do not add a new wallet_transactions type value, per RESEARCH Pitfall 4).
8. Return from the transaction closure: `{ cashout, wagerId, marketId: market.id, userId, question: market.question, netValue: net, grossValue: gross, feeAmount: fee, stakeCashedOut: requestedStake, remainingStake: remainingStake - requestedStake }`.
After `await transaction(...)` resolves (never inside the closure — D-01), emit `domainEvents.emit('wager.cashed_out', { cashoutId: result.cashout.id, wagerId: result.wagerId, userId: result.userId, marketId: result.marketId, question: result.question, netValue: result.netValue, grossValue: result.grossValue, feeAmount: result.feeAmount, stakeCashedOut: result.stakeCashedOut, remainingStake: result.remainingStake })`. Return the cashout-shaped result to the caller (the `result` object, or `result.cashout` merged with `remainingStake` — pick one shape and use it consistently; the controller in Plan 02-06 will `res.json(...)` it directly). Never read, filter, or branch on the wager's binary yes/no selection field anywhere in this method — the formula only needs `amount`, `odds_at_time`, `cashed_out_amount`, and status fields (CASHOUT-09). On an idempotent-replay path, the same emit still fires (the notification listener's own idempotency, per Plan 02-05, silently no-ops a duplicate `relatedId`) — do not add special-case branching to suppress it; this matches this codebase's existing "emit is best-effort, consumer owns dedup" convention.</action>
  <verify>
    <automated>node --check src/services/wagerService.js && node --check src/repositories/wagerRepository.js && node -e "const src=require('fs').readFileSync('src/services/wagerService.js','utf8');const start=src.indexOf('async cashoutWager(');if(start===-1)throw new Error('cashoutWager not found');const rest=src.slice(start);const nextRel=rest.indexOf('\n  async ',10);const region=nextRel===-1?rest:rest.slice(0,nextRel);if(/\.choice\b/.test(region))throw new Error('cashoutWager references the yes/no selection field — must be market-type-agnostic (CASHOUT-09)');if(!/findByIdForUpdate/.test(region))throw new Error('missing lock helper usage');if(!/23505/.test(region))throw new Error('missing idempotency catch');console.log('OK')"</automated>
  </verify>
  <done>cashoutWager validates input, locks market→wager→wallet in order, revalidates after locking, computes gross/fee/net via money.js, persists idempotently (23505-catch-and-replay), credits the wallet with a matching audit record, and emits after commit; incrementCashedOutAmount added to wagerRepository; region-scoped grep confirms no yes/no-selection-field reference inside cashoutWager.</done>
</task>

<task type="auto">
  <name>Task 2: Write cashout.computation.test.js and cashout.validation.test.js</name>
  <files>tests/cashout.computation.test.js, tests/cashout.validation.test.js</files>
  <action>Following the exact setup precedent of tests/notifications.emission.test.js (assertTestDatabase-guarded beforeAll applying applyBaseSchema/applyWalletSchema/applyCashoutMigration, seedTestUser, seedWallet, seedOpenMarket; per-test cleanup; afterAll closePool), create two files driving the real, committed `wagerService.cashoutWager`:

tests/cashout.computation.test.js (CASHOUT-01, CASHOUT-02, CASHOUT-09): (a) seed a pending wager (amount=100, odds_at_time=2, choice='yes') and cash out amount=30 — assert the returned netValue is exactly 60 (30*2, fee=0 with CASHOUT_FEE_PERCENT default) and grossValue is 60; (b) call cashoutWager with an object that also contains extra attacker-controlled fields alongside amount/idempotencyKey (e.g. a `netValue`/`payout` field with a huge fake number) and assert the computed netValue is still exactly the server-computed value, proving client-submitted monetary values are never read; (c) assert the wager's cashed_out_amount in the DB is now 30 after the call; (d) assert cashing out works identically for a wager with choice='no' (no branching on the selection field — CASHOUT-09).

tests/cashout.validation.test.js (CASHOUT-04, CASHOUT-05, plus the zero-remaining-stake guard): (a) amount=0 and amount=-5 both throw ValidationError before touching the DB; (b) a wager on a market whose status is 'closed' throws ConflictError; (c) a wager whose own status is not 'pending' (e.g. already 'won') throws ConflictError; (d) requesting an amount exactly equal to the wager's remaining stake throws ValidationError (must leave remainder strictly > 0); (e) for each of (b) and (c), assert domainEvents does NOT emit 'wager.cashed_out' (attach a one-shot listener before the call, assert it was never invoked) — proving rejected requests never leak a phantom event, matching D-01. If a live *test*-named Postgres database is unreachable in this sandbox (carried-forward Phase 1 limitation — see STATE.md), compensate exactly as Phase 1 did: write both files to the real interface contract (ready to run against a real test DB) and additionally run a temporary mock-backed jest dry run (deleted before commit) driving the real, committed cashoutWager through both files' scenarios, documenting the result in the plan SUMMARY per Plan 01-04's precedent.</action>
  <verify>
    <automated>npx jest tests/cashout.computation.test.js tests/cashout.validation.test.js</automated>
  </verify>
  <done>Both test files exist, exercise the real cashoutWager method, and cover the formula correctness/client-value-ignored/market-agnostic behaviors (computation) and every rejection path plus the rollback-emits-nothing guarantee (validation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request body → service | Only a stake amount + idempotency key cross this boundary; never a computed monetary value |
| concurrent requests → same wager row | Two or more in-flight requests racing to mutate the same wager/wallet rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Elevation of Privilege | wagerRepository.findByIdForUpdate ownership scoping | high | mitigate | cashoutWager locks the wager via `WHERE id = $1 AND market_id = $2 AND user_id = $3 FOR UPDATE` — a caller can never lock or read another user's wager (IDOR); verified by the region-scoped grep in Task 1 and the existing wager-lock method from Plan 02-02 |
| T-02-04-02 | Tampering | double-spend / race condition | high | mitigate | Fixed market→wager→wallet FOR UPDATE lock order (matching placeWager/resolveMarket/deleteMarket) serializes concurrent cashouts on the same wager; second transaction re-reads post-commit state and is correctly rejected or recomputed — proven functionally by Plan 02-07's concurrency test, structurally guaranteed here by the lock-then-revalidate ordering |
| T-02-04-03 | Repudiation | replay via retried idempotency key | high | mitigate | UNIQUE(wager_id, idempotency_key) + 23505-catch-and-replay-existing-result — a retried request never re-applies the wallet credit or the cashed_out_amount increment; proven functionally by Plan 02-07's idempotency test |
| T-02-04-04 | Tampering | client-submitted monetary value override | high | mitigate | cashoutWager's signature destructures only `{ amount, idempotencyKey }`; the payout is always computed server-side via money.js from `wager.odds_at_time` (never read from the request); verified by computation test (b) |
| T-02-04-05 | Tampering | full-cashout-to-zero bypass | medium | mitigate | `requestedStake >= remainingStake` (strict) rejection ensures a wager's remaining stake can never reach zero via this milestone's partial-only cashout, matching the CASHOUT-V2-01 scope boundary |
</threat_model>

<verification>
- `node --check` on both changed files; region-scoped grep confirms no reference to the wager's binary selection field inside `cashoutWager`, confirms lock-helper usage and 23505 handling present.
- `npx jest tests/cashout.computation.test.js tests/cashout.validation.test.js` green (or compensated per documented sandbox limitation).
</verification>

<success_criteria>
cashoutWager computes the stake-proportional value entirely server-side, enforces every CASHOUT-04/05 rejection path, never reaches zero remaining stake, is idempotent under retry, and is provably market-type-agnostic.
</success_criteria>

<output>
Create `.planning/phases/02-partial-cashout/02-04-SUMMARY.md` when done
</output>
