---
phase: 03-new-market-types
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/services/wagerService.js
  - tests/markets.idor.test.js
autonomous: true
requirements: [MARKET-03, MARKET-06]
must_haves:
  truths:
    - "A user can place a wager on a binary market with choice yes/no exactly as today"
    - "A user can place a wager on an Over/Under or multiple-choice market by option_id, with odds read from the option (never the client)"
    - "An option_id belonging to a different market is rejected — a wager can never reference another market's option (IDOR)"
    - "Placing a wager on a closed/expired market is still rejected for all market types"
  artifacts:
    - "wagerService.placeWager generalized (market_type branch, IDOR-safe option lookup, money.multiply payout)"
    - "tests/markets.idor.test.js (cross-market option_id rejected + non-admin creation rejected + odds-from-server)"
  key_links:
    - "placeWager non-binary branch calls marketOptionRepository.findByIdForMarket(optionId, marketId, client) inside the market lock — the wager-placement IDOR chokepoint (MARKET-06)"
    - "odds sourced from market_options.odds server-side, never from the request body"
---

<objective>
Generalize wagerService.placeWager so wagers can be placed on all three market types, sourcing odds server-side (binary from market.odds_yes/odds_no, new types from the IDOR-verified option). Binary placement stays byte-identical (MARKET-03). Also fixes the pre-existing Math.round payout anti-pattern to money.multiply while this function is being touched (RESEARCH.md Open Question 3 — same output for binary).

Purpose: Deliver wager placement for new market types with the IDOR guard (MARKET-06) enforced at the placement chokepoint, plus the attack-vector tests requisitos.txt mandates.

Output: Generalized placeWager + IDOR/attack test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-new-market-types/03-RESEARCH.md
@.planning/phases/03-new-market-types/03-PATTERNS.md
@src/services/wagerService.js
@src/repositories/marketOptionRepository.js
@src/repositories/wagerRepository.js
@tests/helpers/testDb.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize wagerService.placeWager (option_id branch + IDOR lookup + money.multiply)</name>
  <files>src/services/wagerService.js</files>
  <behavior>
    - binary market: choice must be 'yes' or 'no'; odds from market.odds_yes/odds_no; wager stored with choice set, option_id null — identical outcome to today
    - non-binary market: option_id required and numeric; the option must belong to THIS market (findByIdForMarket) else ValidationError; odds read from the option row; wager stored with option_id set, choice null
    - option_id from a different market rejected (findByIdForMarket returns null) — IDOR (MARKET-06)
    - closed/expired market rejected for all types (existing checks preserved)
    - insufficient balance rejected for all types (existing check preserved)
    - potentialPayout computed via money.multiply(amount, odds) — replacing the raw Math.round anti-pattern, same value for binary
  </behavior>
  <action>
Add option_id to the destructure of placeWager's argument object. Move the binary choice validation (choice must be 'yes'/'no') OUT of the pre-transaction block and INTO a market.market_type === 'binary' branch inside the transaction (it needs the market loaded first). Keep the pre-transaction marketId/amount finiteness checks. Inside the transaction, after loading and lock-checking the market (existing SELECT ... FOR UPDATE, status/closes_at checks unchanged) and the wallet balance check (unchanged): branch on market.market_type — binary keeps the existing choice-to-odds logic (odds = choice==='yes' ? odds_yes : odds_no; chosenChoice = choice; chosenOptionId = null); non-binary does Number(option_id) finiteness check then const option = await marketOptionRepository.findByIdForMarket(optionId, market.id, client) (Pattern 1, MARKET-06), throw ValidationError if null ('Opção inválida para esse mercado.'), odds = Number(option.odds), chosenOptionId = optionId, chosenChoice = null. Replace the potentialPayout line (Math.round(wagerAmount * odds * 100) / 100) with money.multiply(wagerAmount, odds) — require money at top if not already imported. Pass choice: chosenChoice and optionId: chosenOptionId through to wagerRepository.create. Keep the wallet debit + recordTransaction + domainEvents.emit('wager.placed', ...) exactly as today (the emit's choice field can stay; do not restructure the event). Do NOT touch cancelWager, cashoutWager, getMyWagers, getWagersByUsername.
  </action>
  <verify>
    <automated>node -e "const src=require('fs').readFileSync('./src/services/wagerService.js','utf8'); const seg=src.slice(src.indexOf('placeWager'), src.indexOf('cancelWager')); const ok=/market_type/.test(seg) && /findByIdForMarket/.test(seg) && /money\.multiply/.test(seg) && !/Math\.round\(wagerAmount/.test(seg); process.exit(ok?0:1)"</automated>
  </verify>
  <done>placeWager places binary (unchanged), over_under, and multiple_choice wagers; non-binary reads odds from the IDOR-verified option; a cross-market option_id is rejected; the Math.round payout is replaced by money.multiply; cancelWager/cashoutWager are untouched. Live-DB verification at phase gate (03-07).</done>
</task>

<task type="auto">
  <name>Task 2: Write IDOR and attack-vector test suite</name>
  <files>tests/markets.idor.test.js</files>
  <action>
Follow tests/cashout.validation.test.js structure. Seed two markets (A and B) each via seedOpenMarket with marketType 'multiple_choice' and options via seedMarketOptions. Assert: (1) placing a wager on market B using an option_id that belongs to market A is rejected (IDOR, MARKET-06) — findByIdForMarket returns null → ValidationError; (2) placing a wager on market B with one of B's own option_ids succeeds and stores odds equal to that option's odds (proves odds come from the server, not the request); (3) a wager whose request body tries to smuggle its own odds/potential_payout has those ignored (odds_at_time equals the option's server odds regardless). Add an attack-vector assertion that a non-admin cannot create a market: assert the route POST /api/markets carries requireAdmin (import src/routes/markets.js and verify the middleware stack includes requireAdmin), OR, if a request-level harness is available, that a non-admin token is rejected 403 — pick whichever the existing test infra supports and document the choice in a comment. Comment the live-Postgres-at-phase-gate note.
  </action>
  <verify>
    <automated>npx jest tests/markets.idor.test.js --listTests</automated>
  </verify>
  <done>markets.idor.test.js exists and is a valid Jest suite covering: cross-market option_id rejected (IDOR), own-market option accepted with server-sourced odds, client-submitted odds ignored, and non-admin market creation blocked. Live-Postgres execution at the phase gate; mock-backed dry-run compensation permitted per Phase 1/2 convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → placeWager | User-submitted option_id and amount cross here; option ownership and odds must be derived server-side |
| service → marketOptionRepository | The option_id/market_id pairing is verified in the repository WHERE clause |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Information Disclosure / Tampering | placeWager option_id | high | mitigate | findByIdForMarket(optionId, market.id, client) inside the market lock — an option from another market resolves to null and is rejected (MARKET-06) |
| T-03-15 | Tampering | odds / potential_payout | high | mitigate | odds read from market_options.odds (or market.odds_yes/no) server-side; payout via money.multiply; request-body odds/payout never read (asserted in tests) |
| T-03-16 | Tampering | choice XOR option_id | medium | mitigate | Service sets exactly one of choice/optionId; DB XOR CHECK (005) is the backstop against any dual/empty selection |
| T-03-17 | Race Condition | concurrent placement vs resolution | medium | mitigate | Option lookup happens inside the existing market FOR UPDATE lock scope, not outside it — reuses the proven Phase 2 lock ordering, no new race surface |
</threat_model>

<verification>
- placeWager handles all three types with server-sourced odds and the IDOR guard.
- Binary placement unchanged; Math.round anti-pattern removed.
- Attack vectors (cross-market option, non-admin creation, client odds) covered by tests.
</verification>

<success_criteria>
- MARKET-06 enforced at wager placement; MARKET-03 preserved.
- requisitos.txt attack-vector scenarios encoded as tests.
</success_criteria>

<output>
Create `.planning/phases/03-new-market-types/03-04-SUMMARY.md` when done.
</output>
