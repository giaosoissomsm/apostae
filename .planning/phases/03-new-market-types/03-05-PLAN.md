---
phase: 03-new-market-types
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - src/services/marketService.js
  - src/controllers/marketsController.js
  - tests/markets.resolution.test.js
autonomous: true
requirements: [MARKET-03, MARKET-06, MARKET-07]
must_haves:
  truths:
    - "A binary market resolves and pays out exactly as today (byte-identical win/payout/audit logic)"
    - "An Over/Under or multiple-choice market resolves by winning_option_id; every wager whose option_id matches the winner is paid, all others marked lost"
    - "The winning_option_id is verified to belong to the market being resolved before any payout (IDOR)"
    - "The cashed_out_amount payout-scaling fix (Phase 2) still holds under the new win-check for all market types"
    - "The market.resolved notification shows the winning option's LABEL, not a raw option id"
  artifacts:
    - "marketService.resolveMarket generalized (single isWinner branch, resolveWithOption, label-in-event)"
    - "marketsController.resolveMarket passes winning_option_id through"
    - "tests/markets.resolution.test.js (payout per type + cashed-out regression + resolution IDOR)"
  key_links:
    - "isWinner predicate is the ONLY changed line inside the payout loop — money.multiply/wallet/audit code stays character-for-character identical (Phase 2 CR-01/02/03 lesson)"
    - "winning option label resolved before domainEvents.emit('market.resolved') (Pitfall 5)"
---

<objective>
Generalize marketService.resolveMarket to pay out N outcomes through the SAME transaction/lock/payout/audit path Phase 2 shipped — changing ONLY the win-check predicate and the market-write call, leaving the money math untouched. This is the phase's highest-risk change: Phase 2's review found 3 critical double-pay bugs (CR-01/02/03) from touching more of a financial function than necessary. This plan does the opposite — a surgical single-branch generalization plus an exhaustive completeness audit of every remaining binary assumption in src/.

Purpose: MARKET-07 (generalized resolution) and the resolution half of MARKET-06 (winning_option_id IDOR), while guaranteeing MARKET-03 (binary payout unchanged) and not repeating the Phase 2 "second code path" failure.

Output: Generalized resolveMarket + controller passthrough + resolution tests + a documented grep audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-new-market-types/03-RESEARCH.md
@.planning/phases/03-new-market-types/03-PATTERNS.md
@.planning/phases/02-partial-cashout/02-REVIEW.md
@src/services/marketService.js
@src/controllers/marketsController.js
@src/repositories/marketOptionRepository.js
@src/repositories/marketRepository.js
@tests/helpers/testDb.js
@tests/cashout.resolution-integration.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize resolveMarket — isWinner branch, resolveWithOption, label-in-event</name>
  <files>src/services/marketService.js</files>
  <behavior>
    - binary: outcome must be 'yes'/'no'; markets.outcome written via existing marketRepository.resolve; wager wins iff wager.choice === outcome; payout/audit identical to today
    - non-binary: winning_option_id required; the option must belong to THIS market (findByIdForMarket) else ValidationError/IDOR reject; markets.winning_option_id written via resolveWithOption; wager wins iff wager.option_id === winningOptionId
    - a wager with a prior partial cashout still pays only remainingFraction of potential_payout (Phase 2 fix holds under the new branch, for every type)
    - market.resolved event carries the winning option's LABEL (non-binary) or the raw 'yes'/'no' (binary) as outcome
    - already-resolved market still rejected (ConflictError)
  </behavior>
  <action>
Accept both outcome and winningOptionId (the controller will pass winning_option_id). At the top, branch validation: binary requires outcome in {'yes','no'} (existing check); non-binary requires a finite winningOptionId. Inside the transaction, after locking the market (existing FOR UPDATE + already-resolved check): branch the market-WRITE — binary calls the unchanged marketRepository.resolve(marketId, outcome, client); non-binary first calls marketOptionRepository.findByIdForMarket(winningOptionId, marketId, client) (MARKET-06 — reject with ValidationError if null), captures option.label, then calls marketRepository.resolveWithOption(marketId, winningOptionId, client). Then, in the EXISTING pending-wagers loop, change ONLY the condition from `if (wager.choice === outcome)` to `const isWinner = market.market_type === 'binary' ? wager.choice === outcome : wager.option_id === winningOptionId; if (isWinner) { ... } else { ... }`. Everything inside both branches — remainingFraction, money.multiply, walletRepository.findByUserIdForUpdate/adjustBalance/recordTransaction, the outcomes.push calls, updateStatus — MUST stay character-for-character identical to the current code (RESEARCH.md Pitfall 2 / 02-REVIEW.md CR-01/02/03). For the emitted market.resolved event (Pitfall 5): pass outcome as the winning option's label string for non-binary types, and the unchanged raw 'yes'/'no' for binary — do not change the wager.won/wager.lost emit shapes. Do NOT modify createMarket (done in 03-03), closeMarket, or deleteMarket in this task. A diff that touches any line inside the payout loop other than the win-check condition is a red flag — keep the change surgical.
  </action>
  <verify>
    <automated>node -e "const src=require('fs').readFileSync('./src/services/marketService.js','utf8'); const seg=src.slice(src.indexOf('async resolveMarket'), src.indexOf('async deleteMarket')); const ok=/isWinner/.test(seg) && /resolveWithOption/.test(seg) && /findByIdForMarket/.test(seg) && /remainingFraction/.test(seg) && /money\.multiply\(wager\.potential_payout, remainingFraction\)/.test(seg); process.exit(ok?0:1)"</automated>
  </verify>
  <done>resolveMarket resolves all three types through one payout path; the win-check is the only changed line inside the loop; money/wallet/audit code is byte-identical; winning_option_id is IDOR-verified; the resolved event carries the option label for new types. Live-DB verification at phase gate (03-07).</done>
</task>

<task type="auto">
  <name>Task 2: Controller passthrough + exhaustive binary-assumption completeness audit</name>
  <files>src/controllers/marketsController.js</files>
  <action>
Update marketsController.resolveMarket to pass winning_option_id through alongside outcome: destructure both from req.body and call marketService.resolveMarket(Number(req.params.id), outcome, winningOptionId) (keep destructure-and-forward; never spread req.body). No other controller change (createMarket/placeWager controllers already forward the whole body to their services, which destructure — matching the mass-assignment convention). THEN perform the Phase-2-lesson completeness audit and record its results in the SUMMARY: grep the entire src/ tree for every remaining binary assumption and confirm each is either (a) correctly branched now, or (b) intentionally binary-only and safe. Run and document: `grep -rn "\.choice" src/` (expect only wagerRepository SELECT column list + resolveMarket's now-branched isWinner — no unbranched wager.choice comparison remains in a payout/refund path), `grep -rn "odds_yes\|odds_no" src/` (expect only migration 001, marketRepository.create's binary INSERT, wagerService binary branch, marketService createMarket binary branch — no non-binary path reads these), and `grep -rn "cashed_out_amount" src/` (confirm resolveMarket AND cancelWager AND deleteMarket each still scale by remainingFraction/remainingStake — the three payout/refund paths Phase 2's CR-02/CR-03 identified — and that the new resolveMarket branch did not regress any of them). If any grep reveals an unbranched binary assumption in a money/resolution/refund path, fix it in this plan (do not defer). The SUMMARY must list each grep's hits with a one-line disposition per hit.
  </action>
  <verify>
    <automated>node -e "const src=require('fs').readFileSync('./src/controllers/marketsController.js','utf8'); const ok=/winning_option_id/.test(src) && /resolveMarket/.test(src); process.exit(ok?0:1)" && grep -rn 'cashed_out_amount' src/services/wagerService.js src/services/marketService.js</automated>
  </verify>
  <done>Controller forwards winning_option_id (destructure-only, no spread). The completeness grep audit is run and every .choice / odds_yes / odds_no / cashed_out_amount hit in src/ has a documented disposition in the SUMMARY; all three payout/refund paths (resolveMarket, cancelWager, deleteMarket) confirmed still cashed_out_amount-aware; no unbranched binary assumption remains in any money path.</done>
</task>

<task type="auto">
  <name>Task 3: Write N-outcome resolution test suite (incl. cashed-out regression + resolution IDOR)</name>
  <files>tests/markets.resolution.test.js</files>
  <action>
Follow tests/cashout.resolution-integration.test.js structure. Cover MARKET-07: (a) binary market resolves and the winning-choice wager is credited its full potential_payout, losers marked lost (regression — same numbers as today); (b) an over_under market resolves by winning_option_id — the wager on the winning option is paid (amount * option odds), the wager on the losing option is marked lost; (c) a multiple_choice market with 3+ options resolves — only the winning-option wagers are paid; (d) CRITICAL cashed-out regression: seed a non-binary wager with cashedOutAmount > 0 (via the extended seedWager), resolve it as the winner, assert the payout equals remainingFraction * potential_payout (NOT the full payout) — proving the Phase 2 double-pay fix holds under the new branch for new types; (e) resolution IDOR (MARKET-06): resolving market B with a winning_option_id that belongs to market A is rejected before any wallet write. Assert wallet balances and wallet_transactions rows, mirroring the existing resolution-integration assertions. Comment the live-Postgres-at-phase-gate note.
  </action>
  <verify>
    <automated>npx jest tests/markets.resolution.test.js --listTests</automated>
  </verify>
  <done>markets.resolution.test.js exists and is a valid Jest suite covering binary regression, over_under and multiple_choice payout, the cashed-out-then-resolved regression for new types, and resolution IDOR rejection. Live-Postgres execution at the phase gate; mock-backed dry-run compensation permitted per Phase 1/2 convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → resolveMarket | Admin-submitted winning_option_id crosses here; must be verified to belong to the market before any payout |
| service → wallet | Payout credits money; a wrong win-check or a lost cashed_out_amount scaling manufactures value from nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-18 | Information Disclosure / Tampering | winning_option_id | high | mitigate | findByIdForMarket(winningOptionId, marketId, client) verifies market ownership before writing winning_option_id or paying anyone (MARKET-06) |
| T-03-19 | Tampering | payout math under new branch | high | mitigate | Only the isWinner condition changes; remainingFraction/money.multiply/wallet-audit stay byte-identical; a dedicated cashed-out regression test guards the Phase 2 CR-02/03 bug class for new types |
| T-03-20 | Information Disclosure | market.resolved notification | medium | mitigate | Winning option label resolved server-side and emitted instead of a raw id (Pitfall 5) — no meaningless "resultado 14" leaked to users |
| T-03-21 | Tampering | mass assignment on resolve | medium | mitigate | Controller destructures {outcome, winning_option_id} explicitly — never spreads req.body into the service/repository |
| T-03-22 | Race Condition | resolve vs placement | medium | accept | Win-check and option verification occur inside the existing market FOR UPDATE lock scope; reuses the proven Phase 2 lock order — no new race surface |
</threat_model>

<verification>
- Resolution generalized surgically; binary payout byte-identical.
- Completeness audit run and documented — the explicit guard against the Phase 2 "second code path" failure.
- Cashed-out regression + resolution IDOR encoded as tests.
</verification>

<success_criteria>
- MARKET-07 satisfied; MARKET-06 enforced at resolution; MARKET-03 preserved.
- No unbranched binary assumption remains in any payout/refund path (audited).
</success_criteria>

<output>
Create `.planning/phases/03-new-market-types/03-05-SUMMARY.md` when done. The SUMMARY MUST include the completeness-audit grep results with per-hit dispositions.
</output>
