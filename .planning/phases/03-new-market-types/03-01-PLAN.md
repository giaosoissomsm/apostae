---
phase: 03-new-market-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/migrations/005_market_types.js
  - tests/helpers/testDb.js
autonomous: true
requirements: [MARKET-01, MARKET-02, MARKET-03, MARKET-06]
must_haves:
  truths:
    - "A market can store a market_type discriminator ('binary'|'over_under'|'multiple_choice'), a numeric threshold, and a winning_option_id"
    - "A market can own N selectable outcomes as market_options rows, each with a label and admin-set odds, FK'd to the parent market"
    - "A wager references EITHER choice (binary) OR option_id (new types) — never both, never neither — enforced by the database itself"
    - "Every pre-existing binary market/wager row keeps working unchanged after the migration (market_type defaults to 'binary')"
  artifacts:
    - "src/migrations/005_market_types.js (additive up/down migration)"
    - "tests/helpers/testDb.js extended with applyMarketTypesMigration + seedMarketOptions + generalized seedOpenMarket/seedWager"
  key_links:
    - "market_options.market_id FK REFERENCES markets(id) ON DELETE CASCADE — the option-to-market ownership the IDOR guard (MARKET-06) queries against"
    - "wagers CHECK constraint (choice XOR option_id) — DB-level defense-in-depth so no code path can create an ambiguous wager"
---

<objective>
Ship the additive schema that lets every downstream layer represent N-outcome markets, plus the test-DB helper extensions every Phase 3 integration test will reuse. This is a purely additive migration (005): zero existing rows change semantics, binary markets keep every column and CHECK they have today (MARKET-03).

Purpose: Establish the storage foundation (market_type, threshold, market_options, winning_option_id, wagers.option_id + XOR CHECK) that MARKET-01/02/06/07 all build on, and give later plans deterministic seed helpers so their tests never re-implement schema setup.

Output: Migration 005 (auto-discovered by scripts/migrate.js's readdirSync+sort — no wiring change needed) and an extended tests/helpers/testDb.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-new-market-types/03-RESEARCH.md
@.planning/phases/03-new-market-types/03-PATTERNS.md
@src/migrations/004_cashout.js
@tests/helpers/testDb.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create additive migration 005_market_types.js</name>
  <files>src/migrations/005_market_types.js</files>
  <action>
Create the migration following 004_cashout.js's exact module shape (const migrations = [ SQL strings ]; module.exports = { id, up: migrations, down: [reverse order] }). Use RESEARCH.md's "Migration 005" Code Examples block verbatim — it was drafted against this exact analog. The up array must, in order: (1) ALTER TABLE markets ADD COLUMN IF NOT EXISTS market_type VARCHAR(20) NOT NULL DEFAULT 'binary' with CHECK (market_type IN ('binary','over_under','multiple_choice')); (2) ADD COLUMN IF NOT EXISTS threshold NUMERIC(10,2) (nullable); (3) CREATE TABLE IF NOT EXISTS market_options (id SERIAL PK, market_id INTEGER NOT NULL REFERENCES markets(id) ON DELETE CASCADE, label VARCHAR(200) NOT NULL, odds NUMERIC(10,2) NOT NULL CHECK (odds >= 1.01 AND odds <= 1000), sort_order INTEGER NOT NULL DEFAULT 0, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, UNIQUE (market_id, label)) plus idx_market_options_market_id; (4) ADD COLUMN IF NOT EXISTS winning_option_id INTEGER REFERENCES market_options(id); (5) ALTER wagers ALTER COLUMN choice DROP NOT NULL; (6) ADD COLUMN IF NOT EXISTS option_id INTEGER REFERENCES market_options(id); (7) ADD CONSTRAINT wagers_choice_xor_option CHECK ((choice IS NOT NULL AND option_id IS NULL) OR (choice IS NULL AND option_id IS NOT NULL)); (8) idx_wagers_option_id. Prefix each SQL entry with a short PT-BR comment explaining the business reason, matching 004's style. The down array is the exact reverse: drop the XOR constraint, drop wagers.option_id, re-SET NOT NULL on choice, drop markets.winning_option_id, DROP TABLE market_options, drop markets.threshold, drop markets.market_type. Do NOT add any scheduled_outcome-equivalent column for new types (Pitfall 6 — new types are admin-resolved only this phase). The odds bound (1.01–1000) must match isValidOdds()'s existing range so market_options.odds enforces the same rule odds_yes/odds_no already do.
  </action>
  <verify>
    <automated>node -e "const m=require('./src/migrations/005_market_types'); const a=m.id==='005_market_types', b=Array.isArray(m.up)&&m.up.length>=8, c=Array.isArray(m.down)&&m.down.length>=7; const j=m.up.join(' '); const d=j.includes('market_options')&&j.includes(\"DEFAULT 'binary'\")&&j.includes('wagers_choice_xor_option')&&j.includes('ON DELETE CASCADE'); process.exit(a&&b&&c&&d?0:1)"</automated>
  </verify>
  <done>Migration loads as a valid module; up creates market_type/threshold/market_options/winning_option_id/wagers.option_id + XOR CHECK; down reverses them; market_options.market_id has ON DELETE CASCADE FK; no scheduled_outcome column added for new types. Live-DB execution of the migration is verified at the phase gate (03-07) against real Postgres, per the carried-forward no-test-DB blocker in STATE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/helpers/testDb.js with market-types seed helpers</name>
  <files>tests/helpers/testDb.js</files>
  <action>
Add, mirroring the existing applyCashoutMigration/seedOpenMarket/seedWager helpers: (1) applyMarketTypesMigration() — lazy-require '../../src/migrations/005_market_types' inside the function (not at module top, matching applyNotificationsMigration's convention so the helper parses even if load order shifts), assertTestDatabase() first, run each up SQL sequentially. (2) seedMarketOptions(marketId, options) — bulk-insert rows into market_options (label, odds, sort_order) for the given marketId, returning the inserted rows' ids in order (use a parameterized multi-row INSERT ... RETURNING id, sort_order ORDER BY sort_order; never string-concatenate labels). (3) Extend seedOpenMarket() to accept optional marketType (default 'binary') and threshold (default null), adding them to the INSERT column list — keep the existing binary call path byte-identical when marketType is omitted (default 'binary', threshold null), so every Phase 1/2 test that calls seedOpenMarket() with no new args still works unchanged (MARKET-03 at the test-fixture layer). (4) Extend seedWager() to accept optional optionId (default null) and make choice nullable-passable — when optionId is provided, insert choice=null/option_id=optionId; otherwise keep today's choice-based insert exactly. Export all new/changed helpers in module.exports. Keep the assertTestDatabase() guard at the top of every new function that runs DDL/DML.
  </action>
  <verify>
    <automated>node -e "const h=require('./tests/helpers/testDb'); const ok=['applyMarketTypesMigration','seedMarketOptions','seedOpenMarket','seedWager'].every(f=>typeof h[f]==='function'); process.exit(ok?0:1)"</automated>
  </verify>
  <done>testDb.js parses and exports applyMarketTypesMigration, seedMarketOptions, and the generalized seedOpenMarket/seedWager. Default-argument calls (no marketType/optionId) produce the identical SQL the current helpers produce (binary regression at the fixture layer). Each new DDL/DML helper calls assertTestDatabase() first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | DDL defines the constraints every later layer relies on; a missing constraint here is a silent security/integrity hole downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | wagers row shape | high | mitigate | DB-level XOR CHECK (choice XOR option_id) guarantees no code path — even a future one — can persist an ambiguous/dual-selection wager; not left to application logic alone (Pitfall 1) |
| T-03-02 | Information Disclosure | market_options ownership | high | mitigate | market_options.market_id NOT NULL FK with ON DELETE CASCADE establishes the parent-ownership the MARKET-06 IDOR query (WHERE id=$1 AND market_id=$2) enforces in plan 03-02/03-04/03-05 |
| T-03-03 | Tampering | odds field | medium | mitigate | market_options.odds CHECK (1.01–1000) enforces the same server-side bound as odds_yes/odds_no at the DB layer, independent of application validation |
| T-03-04 | Denial of Service | option row count | low | accept | Row-count bound is enforced in the service layer (03-03, MARKET-05); the DB table itself is intentionally uncapped — accepted because the write path is admin-only and bounded upstream |
</threat_model>

<verification>
- Migration module loads and exposes valid up/down arrays with all seven schema changes.
- Down array is the exact reverse of up (safe rollback).
- testDb.js helpers load and default-arg calls preserve current binary SQL.
- Full migration execution against live Postgres deferred to phase gate (03-07) — carried-forward no-test-DB blocker (STATE.md).
</verification>

<success_criteria>
- migration 005 present, additive, auto-discoverable, reversible.
- test helpers ready for every downstream Phase 3 test to seed options and non-binary markets.
- Zero change to how binary markets/wagers are stored.
</success_criteria>

<output>
Create `.planning/phases/03-new-market-types/03-01-SUMMARY.md` when done.
</output>
