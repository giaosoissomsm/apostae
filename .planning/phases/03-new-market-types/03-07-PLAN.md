---
phase: 03-new-market-types
plan: 07
type: execute
wave: 6
depends_on: ["03-02", "03-04", "03-06"]
files_modified:
  - public/js/dashboard.js
autonomous: false
requirements: [MARKET-02, MARKET-03, MARKET-07, MARKET-08]
user_setup:
  - service: postgres
    why: "Phase-gate verification requires the full Jest suite to run against a real *test*-named Postgres DB (the carried-forward no-test-DB blocker from Phase 1/2 — mocks passed but hid 3 real financial bugs in Phase 2)"
    dashboard_config:
      - task: "Provide a reachable apostae_test Postgres database (DB_* env vars, NODE_ENV=test) and run migrations 001-005 against it"
        location: "DB host pg_hba/proxy allowlist or a local test Postgres"
must_haves:
  truths:
    - "A binary market ticket renders exactly as today (2 Sim/Não buttons, unchanged markup)"
    - "An Over/Under or multiple-choice market ticket renders one button per option from market.options[]"
    - "3+ option tickets wrap responsively (.odds-row.multi)"
    - "Placing a wager on a non-binary market submits option_id; my-wagers rows show the option label"
    - "All three market types work end-to-end against a real Postgres database"
  artifacts:
    - "public/js/dashboard.js (ticketTemplate branch, selection, submit payload, both choice-label call sites)"
    - "Phase-gate verification record (full suite green against real Postgres + manual 3-type walkthrough)"
  key_links:
    - "market.options[] (from findAll json_agg) drives N option buttons; binary path is a separate branch, untouched"
    - "selections[marketId] holds 'yes'/'no' (binary) or option id (non-binary); submit payload branches accordingly"
---

<objective>
Render N-outcome market tickets on the user dashboard and submit option-based wagers, then run the phase gate: full test suite against a REAL Postgres database plus a manual end-to-end walkthrough of all three market types. Binary tickets render byte-identical to today (MARKET-03 regression guard). This plan is the final MVP slice — after it, a real user can view and bet on Over/Under and multiple-choice markets — and the checkpoint is where the no-live-DB blocker (carried from Phase 1/2) is finally closed.

Purpose: Complete MARKET-08's user-facing half and gate the phase on real-DB verification, directly answering Phase 2's lesson that mock-only verification hid critical financial bugs.

Output: Updated dashboard.js + a phase-gate verification record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-new-market-types/03-UI-SPEC.md
@.planning/phases/03-new-market-types/03-PATTERNS.md
@public/js/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch dashboard.js — ticketTemplate, selection, submit payload, choice-label call sites</name>
  <files>public/js/dashboard.js</files>
  <action>
(1) ticketTemplate(): when market.market_type === 'binary' (or the field is absent — treat as binary for backward compat), render the EXISTING two .odd-btn.sim/.odd-btn.nao buttons block byte-for-byte, including the plain (non-.multi) .odds-row — do not let generalization alter binary markup (UI-SPEC regression guard). When market.market_type !== 'binary', render an if/else sibling branch that maps market.options[] to one .odd-btn.option button each (data-option-id="{o.id}" instead of data-choice; .label = escapeHtml(o.label); .value = o.odds.toFixed(2)+'x'; apply the 'selected' class when selections[marketId] === o.id), and add the .multi modifier to .odds-row when market.options.length > 2. Produce two distinct inner-HTML strings assigned to the same wrapper — do NOT build one shared per-attribute-conditional template. (2) Selection handler: the existing .odd-btn querySelectorAll loop stays; change selections[marketId] = btn.dataset.choice to read btn.dataset.choice ?? Number(btn.dataset.optionId) so it holds a string for binary and a number for non-binary. (3) Submit handler: branch the payload — binary sends { market_id, choice, amount } unchanged; non-binary sends { market_id, option_id, amount } (option_id = the numeric selection). Reuse the existing Api.post/showToast/catch convention verbatim. (4) BOTH choice-label call sites (loadMyWagers row ~183 and showUserWagers ~286, the two of the four Pitfall-4 sites that live in this file): change `w.choice === 'yes' ? 'Sim' : 'Não'` to `w.option_id != null ? escapeHtml(w.option_label) : (w.choice === 'yes' ? 'Sim' : 'Não')` — option_label comes from the extended SELECT_WITH_MARKET join (03-02). Leave every other part of these functions unchanged.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('./public/js/dashboard.js','utf8'); const ok=/market_type/.test(s)&&/data-option-id/.test(s)&&/option_id/.test(s)&&/odds-row.*multi|multi/.test(s)&&/option_label/.test(s)&&(s.match(/option_label/g)||[]).length>=1; const both=(s.match(/w\.option_id != null/g)||[]).length>=2; process.exit(ok&&both?0:1)"</automated>
  </verify>
  <done>Binary tickets render unchanged; non-binary tickets render N option buttons (wrapping when >2); selection/submit handle both string choice and numeric option_id; both my-wagers/user-wagers label sites branch on option_id to show the option label. No shared template mutates binary markup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Phase gate — full suite against real Postgres + manual 3-type walkthrough</name>
  <action>Automate everything first (all suites written and passing under mock-backed dry runs), then hand off to the human to run the full suite against a real *test*-named Postgres and walk through all three market types end-to-end per the steps below. This is a blocking human checkpoint — do not self-approve.</action>
  <what-built>
The complete New Market Types feature across all layers: additive schema (005), N-outcome repositories/services, generalized createMarket/placeWager/resolveMarket, admin creation+resolve UI, and dashboard N-option rendering. All Phase 3 test suites (markets.creation, markets.validation, markets.idor, markets.resolution) plus the full Phase 1/2 regression suite are written.
  </what-built>
  <how-to-verify>
This checkpoint closes the carried-forward no-live-DB blocker (STATE.md) — Phase 2 shipped 3 critical financial bugs that mocks passed but real Postgres would have caught. It must run against a REAL database.

1. Point DB_* env vars at a reachable *test*-named Postgres (e.g. apostae_test), NODE_ENV=test, and run migrations 001–005 against it. Confirm migration 005 applies cleanly (market_options table, market_type/threshold/winning_option_id columns, wagers.option_id + XOR CHECK) and that its `down` reverses cleanly on a throwaway copy.
2. Run the full suite against that DB: `npm test`. REQUIRED: all Phase 3 suites (markets.creation/validation/idor/resolution) AND all Phase 1/2 suites (notifications.*, cashout.*, money) pass green. Any red is a blocker — do not approve.
3. In the browser, as an admin: create a binary market (confirm the form and resulting ticket look identical to before), an Over/Under market (threshold 2.5 + Odds Over/Under), and a multiple-choice market with 4 options. Confirm the admin list shows the right type/odds for each with no console errors.
4. As a user: place a wager on each of the three markets (binary via Sim/Não, the other two via option buttons); confirm my-wagers shows the correct option label; confirm insufficient-balance and closed-market rejections still work.
5. As an admin: resolve each market (binary via Sim/Não buttons; the others via the option dropdown + Resolver); confirm winners are credited, losers marked lost, and the resolution notification shows the option LABEL (not a raw id) for the new types.
6. Attack spot-check: attempt (via devtools/curl) to place a wager on market B using an option_id from market A → must be rejected; submit a multiple-choice create with 21 options bypassing the UI → must be rejected server-side.
  </how-to-verify>
  <resume-signal>Type "approved" once the full suite is green against real Postgres and all three market types work end-to-end, or describe the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user browser → API | Option ids are echoed from server-provided market.options[], never constructed by the client; server re-verifies ownership (MARKET-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-27 | Injection (XSS) | option labels + option_label in tickets/wager rows | high | mitigate | escapeHtml applied to every label/option_label rendered on the dashboard |
| T-03-28 | Tampering | client-submitted option_id | high | mitigate | Server re-verifies the option belongs to the market at placement (03-04, MARKET-06); the dashboard only echoes server-provided ids |
| T-03-29 | Tampering | binary ticket regression | medium | mitigate | Binary branch of ticketTemplate + both label sites kept byte-identical; verified in the manual walkthrough (MARKET-03) |
| T-03-30 | Tampering | mock-hidden financial bug | high | mitigate | Phase-gate checkpoint requires the full suite to pass against REAL Postgres — the explicit countermeasure to Phase 2's mock-only-verification failure |
</threat_model>

<verification>
- Dashboard renders and submits correctly for all three types; binary unchanged.
- Full suite green against real Postgres (blocker if not).
- All three types verified end-to-end in the browser incl. attack spot-checks.
</verification>

<success_criteria>
- MARKET-08 fully met (user-facing); MARKET-02/03/07 verified live.
- The no-live-DB blocker is closed for Phase 3; no mock-only financial claim remains.
</success_criteria>

<output>
Create `.planning/phases/03-new-market-types/03-07-SUMMARY.md` when done, recording the real-Postgres suite result and the manual walkthrough outcome.
</output>
