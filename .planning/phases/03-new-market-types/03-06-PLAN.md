---
phase: 03-new-market-types
plan: 06
type: execute
wave: 5
depends_on: ["03-03", "03-05"]
files_modified:
  - public/admin.html
  - public/js/admin.js
  - public/css/style.css
autonomous: true
requirements: [MARKET-01, MARKET-02, MARKET-03, MARKET-08]
must_haves:
  truths:
    - "The admin creation form has a market-type selector that shows the binary, Over/Under, or multiple-choice fieldset"
    - "The Over/Under fieldset has 3 inputs: threshold, Odds Over, Odds Under (labels auto-derived, odds admin-entered)"
    - "The multiple-choice fieldset lets the admin add/remove option rows, floored at 2 and capped at 20"
    - "The admin market list shows each market's type and its odds/options without crashing for non-binary markets"
    - "A non-binary market can be resolved from the admin panel via a winning-option dropdown + Resolver button"
    - "The binary creation form and the binary market row/resolve buttons are pixel-identical to today"
  artifacts:
    - "public/admin.html (market-type select + 3 fieldsets + dynamic option builder)"
    - "public/js/admin.js (branched submit payload, type-aware odds cell, non-binary resolve control)"
    - "public/css/style.css (additive: .options-list, .option-row, .odd-btn.option, .odds-row.multi, .resolve-select)"
  key_links:
    - "#mMarketType change toggles exactly one visible fieldset; hidden fieldsets are not submitted"
    - "submit payload branches on market_type per UI-SPEC (odds_yes/no | threshold+odds_over/under | options[])"
---

<objective>
Build the admin-panel UI for creating and resolving all three market types (MARKET-08), strictly following the APPROVED 03-UI-SPEC.md contract. All new CSS is additive; binary form/list/resolve markup stays byte-identical (MARKET-03 regression guard). Client-side checks are UX convenience only — the server (03-03/03-05) remains the sole validation authority (MARKET-04).

Purpose: Make MARKET-01/02 reachable through the admin UI, not API-only (MARKET-08).

Output: Updated admin.html, admin.js, and additive style.css.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-new-market-types/03-UI-SPEC.md
@.planning/phases/03-new-market-types/03-PATTERNS.md
@public/admin.html
@public/js/admin.js
@public/css/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add market-type selector, Over/Under + multiple-choice fieldsets to admin.html; add additive CSS</name>
  <files>public/admin.html, public/css/style.css</files>
  <action>
In admin.html's #newMarketForm, add a "Tipo de mercado" select (#mMarketType) with options "Binário (Sim/Não)" (value binary, default selected), "Over/Under (limite numérico)" (over_under), "Múltipla escolha" (multiple_choice), styled with the existing .field select class. Wrap the existing #mOddsYes/#mOddsNo inputs in a binary fieldset div (visible by default — do NOT change those two inputs, MARKET-03). Add an over_under fieldset (hidden) with #mThreshold (number, step 0.5, min 0.5, label "Limite (ex: 2.5)"), #mOddsOver (label "Odd Over"), #mOddsUnder (label "Odd Under") — the two odds inputs identical in attributes to #mOddsYes/#mOddsNo (step 0.01, min 1.01). Add the .5-suffix helper hint text ("Dica: use .5 (ex: 2.5) pra evitar empate exato.") in the muted hint style. Add a multiple_choice fieldset (hidden) containing a .options-list container, an #addOptionBtn ("+ Adicionar opção", .btn-ghost), and the helper text "Mínimo 2, máximo 20 opções." Reuse .field/.field label/.field input/.grid-2 verbatim for all new inputs. In style.css, APPEND (never edit the existing .odds-row/.odd-btn/.odd-btn.sim/.odd-btn.nao rules — MARKET-03 hard rule) exactly these additive rules per UI-SPEC Component Inventory: .options-list (flex column, gap 8px), .option-row (flex, gap 8px, align-items center), .option-row .remove-option (btn-ghost + destructive var(--nao) coloring), .odd-btn.option (color var(--text)), .odds-row.multi (flex-wrap wrap) with .odds-row.multi .odd-btn.option (flex 1 1 120px, min-width 120px), and .resolve-select (reuse .field select / .inline-form select declarations).
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('./public/admin.html','utf8'); const c=require('fs').readFileSync('./public/css/style.css','utf8'); const okH=/id=.mMarketType/.test(h)&&/id=.mThreshold/.test(h)&&/id=.mOddsOver/.test(h)&&/id=.mOddsUnder/.test(h)&&/options-list/.test(h)&&/addOptionBtn/.test(h); const okC=/\.odd-btn\.option/.test(c)&&/\.odds-row\.multi/.test(c)&&/\.options-list/.test(c)&&/\.option-row/.test(c)&&/\.resolve-select/.test(c); process.exit(okH&&okC?0:1)"</automated>
  </verify>
  <done>admin.html has the type selector, the 3-input Over/Under fieldset, and the dynamic multiple-choice builder; style.css has the 5 additive class groups appended; the existing binary inputs and the .odds-row/.odd-btn/.odd-btn.sim/.odd-btn.nao rules are unedited.</done>
</task>

<task type="auto">
  <name>Task 2: Branch admin.js — type-toggle, dynamic option rows, submit payload, odds cell, resolve control</name>
  <files>public/js/admin.js</files>
  <action>
(1) Type toggle: on #mMarketType change, show exactly one fieldset and hide the other two (hidden fieldsets must not contribute to the submit payload). Selecting multiple_choice seeds .options-list with exactly 2 empty rows (the floor). (2) Dynamic rows: #addOptionBtn appends an .option-row (a text input placeholder "Ex: Time A" + an odds number input placeholder "Odd" step 0.01 min 1.01 + a remove button with aria-label "Remover opção"); disable #addOptionBtn once 20 rows exist; disable each remove button when exactly 2 rows remain (client floor). (3) Submit handler: branch the body construction on #mMarketType.value per UI-SPEC — binary keeps { odds_yes, odds_no } unchanged; over_under sends { market_type:'over_under', threshold, odds_over, odds_under }; multiple_choice sends { market_type:'multiple_choice', options: [{label, odds}, ...] } collected from the .option-row DOM. Keep the existing try/await Api.post('/markets', body)/showToast/reset/loadMarkets catch/showToast(err.message,'error') structure verbatim — server errors surface as-is. Client-side pre-submit guardrail copy (min 2 / max 20 / fill all labels+odds / valid threshold / odds 1.01-1000) is convenience only. (4) loadMarkets odds cell: replace the unconditional m.odds_yes.toFixed(2) line (which throws for non-binary) with a small oddsCell(m) helper that branches on m.market_type — binary "{odds_yes}x / {odds_no}x" unchanged; over_under "Over {threshold}: {oddsOver}x / Under {threshold}: {oddsUnder}x"; multiple_choice comma-joined "{label} {odds}x" per option from m.options. Add a "Tipo" column (existing .tag class, base neutral variant) after Pergunta. (5) Resolve controls: keep the binary two-button data-resolve/data-outcome pattern + its event-delegation loop UNCHANGED for binary markets; for non-binary, render a .resolve-select populated from m.options (value = option id, text = label) + a single "Resolver" ghost button, wired in its own listener that calls Api.put(`/markets/${id}/resolve`, { winning_option_id }). Escape any option label rendered into HTML.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('./public/js/admin.js','utf8'); const ok=/mMarketType/.test(s)&&/over_under/.test(s)&&/multiple_choice/.test(s)&&/winning_option_id/.test(s)&&/addOptionBtn/.test(s)&&/showToast\(err\.message/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <done>The form toggles fieldsets, builds/removes option rows within the 2–20 bounds, and submits the correct per-type payload; loadMarkets renders a type column and a non-crashing odds cell for all types; non-binary markets get a select + Resolver button posting winning_option_id; the binary form, odds cell, and two-button resolve path are unchanged; the existing showToast error convention is reused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → API | Client-side validation is convenience only; the server re-validates everything (MARKET-04). Option labels rendered into the list/table must be escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-23 | Tampering | client-side option bound | high | mitigate | UI 2–20 cap is UX only; server-side bound (03-03) is the security boundary — a client bypassing the UI still hits the server check (MARKET-05) |
| T-03-24 | Injection (XSS) | option labels in list/resolve select | high | mitigate | escapeHtml applied to every option label rendered into the market list and resolve dropdown |
| T-03-25 | Elevation of Privilege | admin routes | high | mitigate | UI is served behind existing auth; POST/PUT market routes enforce requireAdmin server-side regardless of what the client renders |
| T-03-26 | Tampering | binary form/list regression | medium | mitigate | Existing binary inputs, odds cell, and resolve buttons + the .odds-row/.odd-btn CSS rules left byte-identical (MARKET-03 regression guard) |
</threat_model>

<verification>
- Type selector toggles fieldsets; Over/Under has 3 inputs; multiple-choice builder respects 2–20.
- Odds cell and resolve control work for all types without crashing.
- Binary UI paths visibly unchanged.
- Client checks are convenience-only; labels escaped.
</verification>

<success_criteria>
- MARKET-08 met: both new types fully creatable and resolvable from the admin panel.
- MARKET-03 preserved across form, list, and resolve UI.
</success_criteria>

<output>
Create `.planning/phases/03-new-market-types/03-06-SUMMARY.md` when done.
</output>
