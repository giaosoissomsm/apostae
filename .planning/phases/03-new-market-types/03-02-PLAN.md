---
phase: 03-new-market-types
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/repositories/marketOptionRepository.js
  - src/repositories/marketRepository.js
  - src/repositories/wagerRepository.js
autonomous: true
requirements: [MARKET-03, MARKET-06, MARKET-07]
must_haves:
  truths:
    - "An option can be looked up ONLY when paired with its owning market_id in the same query (IDOR-safe) — no bare-id lookup exists for client-driven paths"
    - "Options can be bulk-inserted for a market inside a caller-provided transaction, with fully parameterized labels (no string concatenation)"
    - "GET markets returns each market's options[] in a single query (no N+1); binary markets get options an empty array"
    - "A market can be resolved by winning_option_id with the option's market ownership re-verified before the write"
  artifacts:
    - "src/repositories/marketOptionRepository.js (findByIdForMarket, createMany, findByMarketId)"
    - "marketRepository.findAll (json_agg options), create (marketType/threshold + optional client), resolveWithOption"
    - "wagerRepository SELECT_WITH_MARKET (+option_id, market_type, option_label), create (+optionId)"
  key_links:
    - "marketOptionRepository.findByIdForMarket(id, marketId, client) — the single IDOR chokepoint reused by placeWager (03-04) and resolveMarket (03-05)"
    - "marketRepository.findAll json_agg produces the options array dashboard.js (03-07) renders N buttons from"
---

<objective>
Build the data-access layer for N-outcome markets: a new marketOptionRepository with the IDOR-safe lookup and bulk insert, plus in-place generalization of marketRepository (aggregated options read, type-aware create, option-based resolve) and wagerRepository (option_id column + option_label join). Every binary code path in these files stays byte-identical when the new fields are absent/null (MARKET-03).

Purpose: Provide the exact repository methods the service layer (03-03/03-04/03-05) calls, with MARKET-06's ownership-in-WHERE pattern implemented once so no service can accidentally do a bare option lookup.

Output: One new repository file and two generalized ones.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-new-market-types/03-RESEARCH.md
@.planning/phases/03-new-market-types/03-PATTERNS.md
@src/repositories/marketRepository.js
@src/repositories/wagerRepository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketOptionRepository.js with IDOR-safe lookup and bulk insert</name>
  <files>src/repositories/marketOptionRepository.js</files>
  <action>
Create a singleton-class-instance repository (require query from '../config/database'; class MarketOptionRepository; module.exports a new instance) matching marketRepository/wagerRepository's export shape. Methods: (1) findByIdForMarket(id, marketId, client) — copy wagerRepository.findByIdForUpdate's ownership-in-WHERE shape exactly, dropping userId, selecting from market_options with BOTH id and market_id in the WHERE plus FOR UPDATE; support optional client via a runner fallback so it works inside or outside a transaction. This is the SINGLE IDOR chokepoint (MARKET-06) — provide NO method that looks up an option by id alone for a client-driven path. (2) createMany(marketId, options, client) — bulk parameterized insert into market_options (market_id, label, odds, sort_order), building the positional placeholder tuples programmatically from the options array index (four params per row), returning all inserted columns; never concatenate label/odds values into the SQL string (injection guard, RESEARCH.md Security Domain). Requires a client (runs inside createMarket's transaction). (3) findByMarketId(marketId, client) — plain select ordered by sort_order, optional client. PT-BR JSDoc per convention.
  </action>
  <verify>
    <automated>node -e "const r=require('./src/repositories/marketOptionRepository'); const ok=['findByIdForMarket','createMany','findByMarketId'].every(m=>typeof r[m]==='function'); const src=require('fs').readFileSync('./src/repositories/marketOptionRepository.js','utf8'); process.exit(ok && /market_id = \$2/.test(src) && /FOR UPDATE/.test(src) ? 0 : 1)"</automated>
  </verify>
  <done>marketOptionRepository exports findByIdForMarket/createMany/findByMarketId; findByIdForMarket pairs id with market_id in a FOR UPDATE query; createMany uses positional placeholders (no interpolated labels). No client-facing bare-id option lookup exists.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize marketRepository — findAll (options join), create (type/threshold + client), resolveWithOption</name>
  <files>src/repositories/marketRepository.js</files>
  <action>
(1) findAll(): replace ONLY the SQL string with RESEARCH.md Pattern 3's json_agg aggregation — select m.*, plus a COALESCE(json_agg(json_build_object of id/label/odds/sort_order ORDER BY sort_order) FILTER (WHERE mo.id IS NOT NULL), empty-array literal) AS options, from markets LEFT JOIN market_options ON market_id, GROUP BY m.id, ORDER BY created_at DESC. Keep the method signature and the return of result.rows unchanged; binary markets naturally get an empty options array. (2) create(): add marketType (default 'binary') and threshold (default null) to the destructured params and to the INSERT column list; keep oddsYes/oddsNo nullable-safe (pass null for non-binary) so the binary INSERT stays byte-identical when marketType is 'binary' and threshold is null. Add an optional trailing client param with a runner fallback so createMarket can run the market insert and the option bulk-insert inside one transaction, without breaking existing no-client callers. (3) Add resolveWithOption(id, winningOptionId, client): UPDATE markets SET status resolved, winning_option_id, resolved_at now, WHERE id, RETURNING all. Do NOT modify the existing resolve() method (binary keeps calling it unchanged) or findByIdForUpdate/findDueToReveal/findDueToClose/updateStatus/delete/findById.
  </action>
  <verify>
    <automated>node -e "const r=require('./src/repositories/marketRepository'); const src=require('fs').readFileSync('./src/repositories/marketRepository.js','utf8'); const ok=typeof r.resolveWithOption==='function' && /json_agg/.test(src) && /market_type/.test(src) && /winning_option_id/.test(src) && /async resolve\(id, outcome, client\)/.test(src); process.exit(ok?0:1)"</automated>
  </verify>
  <done>marketRepository.findAll returns markets with an options array (empty for binary) in one query; create accepts marketType/threshold and an optional client while keeping the binary insert identical; resolveWithOption exists; the original binary resolve() is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Generalize wagerRepository — SELECT_WITH_MARKET (option join) + create (optionId)</name>
  <files>src/repositories/wagerRepository.js</files>
  <action>
(1) Extend SELECT_WITH_MARKET per RESEARCH.md Code Examples: add w.option_id to the wager columns, m.market_type to the market columns, and mo.label AS option_label via LEFT JOIN market_options mo ON mo.id = w.option_id. This makes option_label available to findByUserId/findByUsername so the dashboard (03-07) can render the option label instead of the raw id (Pitfall 5). (2) create(): add optionId to the destructure (default null) and to the INSERT column list; choice is now nullable — when optionId is provided, pass choice as null; otherwise keep the existing choice-based insert exactly (the DB XOR CHECK from migration 005 is the backstop). Do NOT touch findPendingByMarket, findByIdForUpdate, updateStatus, incrementCashedOutAmount, findById — those operate on id/amount/cashed_out_amount and are already market-type-agnostic (this is the completeness discipline the phase's Phase-2 lesson demands: change only what references choice, leave the money-agnostic methods alone).
  </action>
  <verify>
    <automated>node -e "const src=require('fs').readFileSync('./src/repositories/wagerRepository.js','utf8'); const ok=/option_label/.test(src) && /m.market_type/.test(src) && /LEFT JOIN market_options/.test(src) && /optionId/.test(src); process.exit(ok?0:1)"</automated>
  </verify>
  <done>SELECT_WITH_MARKET returns option_id, market_type, and option_label; create accepts optionId and inserts choice as null when an option is chosen; the money-agnostic wager methods are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → repository | Services pass a client-influenced option_id/market_id pairing here; the repository query is the last place ownership can be enforced before the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Information Disclosure | marketOptionRepository.findByIdForMarket | high | mitigate | id ALWAYS paired with market_id in the same WHERE + FOR UPDATE; no bare-id client-facing lookup method exists (MARKET-06) |
| T-03-06 | Injection | createMany bulk insert | high | mitigate | Positional placeholders generated from array index; option labels/odds never string-concatenated into SQL |
| T-03-07 | Tampering | marketRepository.create binary path | high | mitigate | Binary INSERT stays byte-identical when marketType='binary'/threshold=null — regression guard so generalization cannot silently alter existing market creation |
| T-03-08 | Information Disclosure | findAll options aggregation | low | accept | json_agg exposes only id/label/odds/sort_order (already public per-market data); no admin-only field (e.g. scheduled_outcome) is joined in — sanitizeMarket in the service still strips admin fields |
</threat_model>

<verification>
- marketOptionRepository: IDOR-safe lookup present, no bare-id client path, parameterized bulk insert.
- marketRepository: single-query options aggregation, type-aware create with preserved binary path, new resolveWithOption, untouched binary resolve.
- wagerRepository: option_label join + optionId create, money-agnostic methods untouched.
- Live-DB query execution deferred to phase gate (03-07) — carried-forward no-test-DB blocker.
</verification>

<success_criteria>
- Every client-driven option lookup goes through the one IDOR-safe method.
- Binary read/write paths in both existing repositories are unchanged.
- The service layer has all repository methods it needs for creation, placement, and resolution.
</success_criteria>

<output>
Create `.planning/phases/03-new-market-types/03-02-SUMMARY.md` when done.
</output>
