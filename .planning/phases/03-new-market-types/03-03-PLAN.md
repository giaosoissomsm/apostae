---
phase: 03-new-market-types
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/services/marketService.js
  - tests/markets.creation.test.js
  - tests/markets.validation.test.js
autonomous: true
requirements: [MARKET-01, MARKET-02, MARKET-03, MARKET-04, MARKET-05]
must_haves:
  truths:
    - "Admin can create an Over/Under market: a numeric threshold plus admin-entered Odds Over and Odds Under, auto-labeled Over X / Under X"
    - "Admin can create a multiple-choice market with 2..20 options, each label unique (case-insensitive) with admin-entered odds"
    - "Creating a binary market behaves exactly as today — same validation, same result shape"
    - "Every market-type/option rule (type enum, option count bound, threshold format, duplicate labels, odds range) is enforced server-side, rejecting bad input regardless of any client UI"
    - "Market row + its options are written atomically in one transaction"
  artifacts:
    - "marketService.createMarket generalized (branch on market_type, transaction-wrapped)"
    - "tests/markets.creation.test.js (positive path per type + binary regression)"
    - "tests/markets.validation.test.js (count/threshold/duplicate/bound rejections)"
  key_links:
    - "createMarket transaction wraps marketRepository.create + marketOptionRepository.createMany — both succeed or neither does"
    - "Server-side 2..20 option bound is independent of any HTML form cap (MARKET-05)"
---

<objective>
Generalize marketService.createMarket to create all three market types with full server-side validation, wrapping the market insert and option bulk-insert in one transaction. Binary creation stays byte-identical (MARKET-03). Honors STATE.md locked decisions: option count bounded 2–20 server-side; Over/Under is a 3-input form (threshold + Odds Over + Odds Under) whose labels auto-generate ("Over X"/"Under X") but whose odds are admin-supplied.

Purpose: Deliver the creation half of the MVP slice — an admin can create Over/Under and multiple-choice markets, validated and bounded server-side (MARKET-01/02/04/05).

Output: Generalized createMarket + creation/validation test suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-new-market-types/03-RESEARCH.md
@.planning/phases/03-new-market-types/03-PATTERNS.md
@.planning/phases/03-new-market-types/03-UI-SPEC.md
@src/services/marketService.js
@src/repositories/marketOptionRepository.js
@src/repositories/marketRepository.js
@tests/helpers/testDb.js
@tests/cashout.validation.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize marketService.createMarket with per-type server-side validation + transaction</name>
  <files>src/services/marketService.js</files>
  <behavior>
    - binary (market_type absent or 'binary'): existing question (>=4 chars) + isValidOdds(odds_yes)/isValidOdds(odds_no) checks unchanged; creates a market with no options; returns the market row exactly as today
    - over_under: threshold must be Number.isFinite and > 0, odds_over/odds_under each isValidOdds; creates a market with threshold set and exactly 2 options labeled "Over {threshold}" (sort_order 0) and "Under {threshold}" (sort_order 1) with the respective admin odds
    - multiple_choice: options array length in [2,20]; each option label a non-empty trimmed string, odds isValidOdds; labels unique after case-insensitive trim (duplicate rejected); creates N option rows in submitted order (sort_order = index)
    - invalid market_type value (not one of the three) rejected with ValidationError
    - all rejections throw ValidationError (PT-BR message) BEFORE any DB write
    - option count 21 rejected server-side even though the DB table is uncapped (MARKET-05)
  </behavior>
  <action>
Branch createMarket on body.market_type (default 'binary' when absent — preserves today's implicit contract, MARKET-03). Keep the existing question and isValidOdds checks verbatim for the binary branch. Add an over_under branch (validate threshold finite and > 0 per STATE.md locked decision; validate odds_over/odds_under via the existing isValidOdds helper; build two option objects: label "Over {threshold}"/"Under {threshold}", odds from odds_over/odds_under, sort_order 0/1 — labels auto-generated from threshold, odds admin-supplied) and a multiple_choice branch (validate options is an array with length between 2 and 20 inclusive — the locked server-side bound; trim+validate each label non-empty and each odds via isValidOdds; reject duplicate labels using a case-insensitive trimmed comparison). Reuse parseSchedule for closes_at/reveal_at, but pass scheduledOutcome=null for non-binary types (Pitfall 6 — new types are admin-resolved only; do not accept a scheduled_outcome for them). Wrap the market insert + option bulk-insert in one transaction (follow the transaction(async (client) => {...}) shape already in resolveMarket/deleteMarket): call marketRepository.create({..., marketType, threshold, oddsYes/oddsNo null for non-binary}, client) then, for non-binary, marketOptionRepository.createMany(market.id, options, client). Return the created market (attach its options for non-binary via the createMany return, or leave as-is — do not change the binary return shape). Keep the logger.info line. Every new validation throws ValidationError, never a raw Error. Do NOT touch resolveMarket, closeMarket, deleteMarket, or the module's helper functions in this task.
  </action>
  <verify>
    <automated>node -e "const s=require('./src/services/marketService'); const src=require('fs').readFileSync('./src/services/marketService.js','utf8'); const ok=/market_type/.test(src) && /over_under/.test(src) && /multiple_choice/.test(src) && /createMany/.test(src) && /transaction\(/.test(src.slice(src.indexOf('createMarket'), src.indexOf('closeMarket'))); process.exit(ok?0:1)"</automated>
  </verify>
  <done>createMarket creates binary (unchanged), over_under (threshold + 2 auto-labeled options), and multiple_choice (2..20 deduped options) markets; all bad input throws ValidationError before any write; market + options are written in one transaction; 21 options and duplicate labels are rejected server-side. Live-DB verification at phase gate (03-07).</done>
</task>

<task type="auto">
  <name>Task 2: Write creation and validation test suites</name>
  <files>tests/markets.creation.test.js, tests/markets.validation.test.js</files>
  <action>
Follow tests/cashout.validation.test.js's structure exactly (beforeAll applies base+wallet+market-types migrations via the extended testDb helpers, seedTestUser for an admin id, afterAll closePool). markets.creation.test.js (MARKET-01/02/03): assert createMarket produces (a) a binary market unchanged (market_type 'binary', no options, same fields as today — regression), (b) an over_under market with threshold set and exactly 2 options labeled "Over 2.5"/"Under 2.5" with the given odds, (c) a multiple_choice market with N (e.g. 4) options in submitted order. markets.validation.test.js (MARKET-04/05): assert ValidationError (or the service's error type) is thrown for — options.length 1 (below floor), options.length 21 (above the locked 20 bound, MARKET-05), duplicate labels differing only by case/whitespace, threshold of 0 / negative / non-numeric, an odds value outside 1.01–1000, and an unknown market_type string. Use the extended seedOpenMarket/seedMarketOptions helpers where a pre-existing market is needed. Note at the top of each file (comment) that live-Postgres execution is the phase gate per the carried-forward no-test-DB blocker.
  </action>
  <verify>
    <automated>npx jest tests/markets.creation.test.js tests/markets.validation.test.js --listTests</automated>
  </verify>
  <done>Both test files exist, are valid Jest suites (listed by --listTests), and cover positive creation per type + binary regression + every negative case in the output requirements (invalid count, duplicate options, malformed threshold, over-20 bound, bad odds, unknown type). Execution against real Postgres is deferred to the phase gate (03-07); mock-backed dry-run compensation permitted per Phase 1/2 convention if attempted earlier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → createMarket | Admin-submitted market_type, threshold, options[] cross into the service; all must be validated/bounded here, never trusted |
| requireAdmin → route | POST /api/markets is gated by existing requireAdmin middleware (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Denial of Service | multiple_choice options[] | high | mitigate | Server-side 2..20 length bound enforced before any DB write, independent of the HTML form cap (MARKET-05) |
| T-03-10 | Tampering | market_type discriminator | high | mitigate | market_type validated against the three-value enum; unknown values rejected with ValidationError (also backstopped by the DB CHECK from 005) |
| T-03-11 | Injection | option labels | high | mitigate | Labels flow through marketOptionRepository.createMany's positional placeholders (03-02); createMarket never builds SQL from label text |
| T-03-12 | Elevation of Privilege | POST /api/markets | high | mitigate | Existing requireAdmin middleware on the route (unchanged); the non-admin-rejected attack vector is asserted in plan 03-04's attack tests |
| T-03-13 | Tampering | Over/Under threshold | medium | mitigate | threshold validated Number.isFinite and > 0 before use; stored as NUMERIC(10,2); labels derived server-side from the validated threshold, not accepted as free text (locked decision) |
</threat_model>

<verification>
- createMarket branches correctly per type with all server-side validation and one atomic transaction.
- Binary creation is provably unchanged (regression test).
- Negative and DoS-bound cases rejected server-side.
</verification>

<success_criteria>
- MARKET-01, MARKET-02, MARKET-04, MARKET-05 satisfied at the service layer; MARKET-03 preserved.
- Tests encode every required positive and negative scenario.
</success_criteria>

<output>
Create `.planning/phases/03-new-market-types/03-03-SUMMARY.md` when done.
</output>
